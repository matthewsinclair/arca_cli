{
  "schema": "intent/thread@3.0",
  "id": "ST0011",
  "title": "Fable review of arca_cli base code",
  "slug": "fable-review-of-arca-cli-base-code",
  "status": "completed",
  "created": "2026-08-04",
  "completed": "2026-08-04T20:10:04Z",
  "objective": "Turn the Fable full-codebase review of arca_cli into a materially better 0.5.0 release: fix every confirmed correctness failure (exit codes, --version, config coercion, renderer side effects, escript-broken commands), delete the dead machinery, and collapse the duplicated concerns -- so the breaking change bundled around issue 0001 pays for itself once.",
  "context": "Issue 0001 (every command exits 0) exposed a failure archetype: correctness information destroyed mid-pipeline. The Fable audit (2026-08-04, full read of all 55 lib/ files at ca7ba57, findings probe-verified) found the same archetype in 11 further confirmed places, plus 10 dead-machinery clusters and 15 design-debt items. The complete catalogue with file:line evidence and probe transcripts is in `design.md`. Remediation is organised as WP-01..WP-10; sequencing and the ruled decisions are in `tasks.md`; the acceptance contract is in `acceptance.md` (RATIFIED by hv, 2026-08-04).\n\nBaseline: 493 tests passing, 0 failures, no compile warnings, at ca7ba57.",
  "body": "## Related Steel Threads\n\n- ST0010: HEREDOC injection for cli.script (the script executor WP-05 hardens)\n- intent/issues/OPEN/0001: exit-code issue that triggered the review (closed by WP-01 + WP-10)",
  "preamble": "",
  "related": [
    {
      "id": "ST0010",
      "note": "HEREDOC injection for cli.script (the script executor WP-05 hardens"
    }
  ],
  "wps": [
    {
      "seq": 1,
      "title": "Exit codes: propagate command outcome to the OS",
      "scope": "S",
      "status": "done",
      "objective": "Close issue 0001: a failed command must exit non-zero. Carry the command outcome from handler to OS boundary without changing any display behaviour. Findings: A1, plus the script/sys.cmd outcome legs of A7/A10 land in their own WPs.",
      "body": "## Deliverables\n\n- Status-carrying dispatch: `process_command_result(%Ctx{})` folds `ctx.status` into the result (making `Ctx.complete/2` load-bearing); `execute_command/5` and a new `dispatch/3` carry `{status, output}`.\n- Legacy adapters: `handle_args/3`, `handle_subcommand/4`, `parse_command_line/3` keep their current string-returning contracts as one-line wrappers over the status-carrying path (REPL and downstream callers unchanged).\n- `Arca.Cli.run/1`: pure entry point -- parses, prints exactly as today, returns `:ok | :error | :warning`. Never halts.\n- `Arca.Cli.main/1`: thin boundary -- `argv |> run() |> halt_for()`. `System.halt(1)` on `:error`; `:warning` exits 0 (hv ruling, 2026-08-04).\n- Delete the dead error branches in `main/1` (`arca_cli.ex:294-311`).\n- Migrate the in-repo test call sites from `main/1` to `run/1` -- both grep forms, qualified `Arca.Cli.main(` and aliased `Cli.main(` (22 sites).\n- `Mix.Tasks.Arca.Cli` (task name `arca.cli`, not `arca_cli`) stays on `main/1` so the mix path inherits the halt.\n- Regression tests assert the REAL process exit code from a subprocess, not the return value.\n- Verify piped stdout is not truncated by the halt (`cmd | cat`, `cmd > file`); use flush-safe halt semantics if it is.\n\n## Dependencies\n\n- None. This WP goes first; several later WPs build on `{status, output}` plumbing.\n- hv ruled both open decisions on 2026-08-04: `:warning` exits 0, and a single exit code 1 for all failures. See `tasks.md` \"Ratified decisions\".",
      "preamble": ""
    },
    {
      "seq": 2,
      "title": "Version truth: single source and working --version",
      "scope": "S",
      "status": "done",
      "objective": "One version, everywhere, and a `--version` flag that prints it. Findings: A2, A3.",
      "body": "## Deliverables\n\n- `VERSION` file becomes the single source: `config/config.exs` `:version` derives from it (or is removed in favour of `Application.spec(:arca_cli, :vsn)`); `BaseConfigurator` default version derives from the consuming app's spec instead of the \"Arca CLI VERSION\" placeholder.\n- `--version` handled explicitly in `command_line_type/1` / `handle_args/3` (Optimus `:version` result routed, not dropped into the help catch-all); prints name + version, exits 0.\n- `about` prints the real version.\n- Placeholder strings (\"Arca CLI ABOUT\" etc) in `DftConfigurator` replaced with real values or values derived from app env, so `--help`'s description block is truthful too.\n- Tests: `--version` output matches `VERSION` content; `about` output matches `VERSION` content.\n\n## Dependencies\n\n- WP-01 (exit-code plumbing in place so `--version` exits 0 through the same path).",
      "preamble": ""
    },
    {
      "seq": 3,
      "title": "Configurator truthfulness: honour config, fail loudly",
      "scope": "S",
      "status": "done",
      "objective": "Configuration that is set must take effect; configuration that is broken must fail loudly. Findings: A4, C3, C2 (collision policy leg).",
      "body": "## Deliverables\n\n- Fix the `|| true` coercions in `BaseConfigurator.__before_compile__` for `allow_unknown_args` and `parse_double_dash` (same nil-only-default treatment the `sorted` fix already has).\n- Coordinator: configurator setup failure raises (or exits non-zero with a clear message) instead of silently substituting `DftConfigurator`'s command set.\n- Duplicate command names across configurators: pick ONE policy (recommended: last-registered wins in BOTH Optimus config and dispatch) and enforce it in `handler_for_command` so parse and dispatch cannot disagree; upgrade the duplicate warning to include the winner.\n- Tests: explicit-false configurator options observable in `config/0` and in Optimus behaviour; broken configurator surfaces an error; duplicate command resolves to the same module in parse and dispatch.\n\n## Dependencies\n\n- None (parallel-safe with WP-01/WP-02).",
      "preamble": ""
    },
    {
      "seq": 4,
      "title": "Pure renderers: one style detector, correct io",
      "scope": "S",
      "status": "done",
      "objective": "Rendering must never change what a command DOES, and the output medium must get the right bytes: unicode content survives pipes, ANSI decoration does not. Findings: A6, A12, C7, C13.",
      "body": "## Deliverables\n\n- Spinner/progress purity: `{:spinner, label, fun}` / `{:progress, label, fun}` executed in the COMMAND layer (or at Ctx build time) with the result stored; renderers only draw. All styles produce the same side effects. Breaking change documented for downstream Ctx users.\n- One style detector: `Output.determine_style/1` is the single authority; `AnsiRenderer.check_tty` deleted; `main/1`'s unconditional `ansi_enabled: true` removed -- ANSI on TTY only (honouring NO_COLOR / ARCA_STYLE as today).\n- Escript unicode io: set stdio encoding (escript `emu_args` or `:io.setopts`) so emoji/unicode print correctly on TTY and piped.\n- Dedupe Ansi/Plain shared logic (table option wrangling, `remove_header_lines`, `safe_to_string`) into one helper; fix drift: `{:list, items}` 2-tuple parity, `{:json, ...}` added to `Ctx.output_item` type and `Ctx.to_string`.\n- Tests: piped output contains no ANSI escapes and intact UTF-8; spinner side effect observable under plain and json styles.\n\n## Dependencies\n\n- WP-01 (shares `main/1` surgery; sequence after to avoid conflict).",
      "preamble": ""
    },
    {
      "seq": 5,
      "title": "History and REPL integrity",
      "scope": "S",
      "status": "done",
      "objective": "History must degrade the way its error handling claims to, and non-interactive paths must be strict. Findings: A5, A7, C8, C9 (history_size leg), C14.",
      "body": "## Deliverables\n\n- Replace illusory `try/rescue` around `GenServer.call` with exit-aware handling (`try/catch :exit` or a `whereis` guard) in all five History client wrappers and the same-pattern call sites (`cli_status`, `sys_flush`); the documented `{:error, :history_not_available, ...}` returns become real.\n- Enforce `history_size` (default 100): History trims on push; setting read from config.\n- Fix `should_push?` substring matching: exact first-token match against the non-history command set.\n- Strict non-interactive execution: `cli.script` and `cli.redo`/`redo` bypass fuzzy matching (exact commands only); fuzzy matching remains a REPL-prompt convenience. Script executor gains stop-on-error default now that WP-01 gives it a real failure signal (flag to continue: `--keep-going`).\n- Cover the latent REPL crashes: 3-tuple error clause in `repl/3`'s else and in `eval_for_redo`.\n- Tests: killed History process yields the documented error tuple (not a crash); history bounded at configured size; `settings.get help_url` lands in history; script with a typo'd command fails instead of running a different command.\n\n## Dependencies\n\n- WP-01 (stop-on-error needs the status signal).",
      "preamble": ""
    },
    {
      "seq": 6,
      "title": "Command hygiene: Ctx API, sys.cmd, dev.*, cli.debug, namespace helper",
      "scope": "S",
      "status": "done",
      "objective": "The shipped commands and command-building helpers must do what they claim in every deployment format. Findings: A8, A9, A10, A11, C1, C4, C12.",
      "body": "## Deliverables\n\n- Ctx API: fix the three in-repo misusers (`settings.all`, `cli.history`, `sys.info`) to `Ctx.new(args, settings, command: :\"...\")`; consider `Ctx.for_command(cmd, args, settings)` convenience so the right call is the easy call; guard `Ctx.new` against atom args.\n- `sys.cmd` rewrite: pass unknown args as SEPARATE arguments (no joining); print once (no tuple leak -- return a Ctx or string, drop the in-handler `print_ansi` side effect); surface the OS exit status via WP-01 plumbing (`sys.cmd false` -> CLI exit non-zero, message names the OS status). Delete the copy-paste `SysCommand` twin (WP-07 dependency noted).\n- `dev.info`: escript-safe -- report from `Application.spec` / `System` info, no `Mix.env()` at runtime; never crashes in escript.\n- `dev.deps`: report the REAL loaded applications (`Application.loaded_applications/0`) or say \"unavailable in this deployment\"; delete the fabricated hardcoded list.\n- `cli.debug`: make persistence real -- load persisted `debug_mode` into app env at startup (delayed-init or first-read); or drop persistence and document it as session-only. Recommended: load at startup.\n- `namespace_command` macro: unwrap the `do:` block correctly (generated `handle/3` returns the block's value, not `[do: value]`); generate modules under the CALLER's namespace, not `Arca.Cli.Commands.*`; strengthen the helper tests to assert exact output (the `=~` assertions masked the wrapper).\n- `BaseSubCommand.extract_arguments`: derive argv from the command's declared arg order, not `Map.values/1` term order; fix the moduledoc (`@sub_commands` attribute style is dead -- config key is the mechanism).\n- `put_lines(map/tuple)`: stop `IO.inspect`-ing at users; render via one explicit path.\n\n## Dependencies\n\n- WP-01 (sys.cmd exit-status leg).",
      "preamble": ""
    },
    {
      "seq": 7,
      "title": "Dead code purge and dependency prune",
      "scope": "S",
      "status": "done",
      "objective": "Delete the machinery that provably does nothing, so the surviving code is the system. Findings: B1-B10, C10.",
      "body": "## Deliverables\n\n- Delete the dead config-callback subsystem (`load_config_phase`, `register_config_callbacks`, `handle_config_change`, `apply_display_settings`, `register_specific_callbacks`) and the `Multiplyer.Config.Server` reference. (If WP-06 makes cli.debug load persisted settings at startup, keep exactly the minimal loader it needs -- built fresh, not this subsystem.)\n- Delete the seven legacy unregistered command modules (Flush, Get, History, Redo, Status, Sys, Settings) and the example SubCommand/OneCommand pair (move the example into docs); changelog maps each to its registered replacement.\n- Delete the unused ErrorHandler location macros + `__using__`.\n- Delete the `REPL_MODE` branches (application start + runtime.exs) or wire the flag properly from `ReplCommand` -- decide with hv; recommended: delete, revisit file-logging as its own feature if wanted.\n- Delete `:is_repl_mode`, `use OK.Pipe` (both sites), commented-out blocks, vestigial `intro/2` params.\n- Prune deps: remove `table_rex`, `pathex`, `elixir_uuid`, `ucwidth`, `certifi`, `castore`, `dotenv`, and `:ok` unless a real usage is found at removal time (`mix xref` check per dep before deletion).\n- Remove or deprecate Utils HTTP residue (`form_encoded_body`, `parse_json_body`, `fetch_body`, `decode_json`) -- public API, so deprecation note in changelog; `form_encoded_body`'s missing URL-encoding documented as the reason it must not be used.\n- mix.exs hygiene: drop the no-op `mix_tasks:` project key and `ansi_enabled:` application key.\n- Gate: full suite green after every deletion batch; `mix xref graph` sanity check.\n\n## Dependencies\n\n- Best sequenced after WP-01..WP-06 land (deletions shrink their conflict surface only if done first, but the safer order is fixes-then-purge so nothing half-dead is being edited).\n- hv ruled both open decisions on 2026-08-04: delete REPL_MODE (revisit file logging as its own feature if wanted), and delete rather than deprecate the public legacy modules, with a changelog map of replacements. See `tasks.md` \"Ratified decisions\".",
      "preamble": ""
    },
    {
      "seq": 8,
      "title": "One error-formatting pipeline",
      "scope": "S",
      "status": "done",
      "objective": "One error dialect, formatted in one place. Findings: C5, C15 (inspect-wrapping leg), plus the REPL deprecated formatters.",
      "body": "## Deliverables\n\n- `ErrorHandler.format_error/2` becomes the single formatter; `Arca.Cli.format_error_with_type`, `error_type_to_prefix`, and the `handle_error/1,2,3` family reduce to thin wrappers (or delegate outright); REPL's deprecated `format_error/2` deleted.\n- One user-visible dialect chosen (recommended: `error: <context>: <message>`, lowercase, no inspect-quotes) and applied across unknown-command, parse-error, command-error, and enhanced-error paths; the command-not-found suggestion block keeps its extra lines.\n- Kill inspect-artifacts in user output (`\"Key not found\"` quotes from `get_setting` and `cfg.get`).\n- Finding A13 -- MUST NOT BE DROPPED (hv, 2026-08-04: \"as long as it is fixed I don't mind when, just do not forget it\"). `settings.get nosuchkey`, `cfg.get nosuchkey` and `cli.redo 999` print a failure but return it as a plain display string, so they still exit 0 after WP-01. Each must return an error tuple (or a Ctx completed `:error`) so the outcome reaches the exit status. This is AC-08.3; the WP-08 close-gate cannot pass until it is satisfied. Doing it here rather than in WP-01 is deliberate: the fix changes the error text, and this is the WP where the ratified dialect lands, so the display changes once.\n- Debug mode appends the structured debug block exactly as today.\n- Tests: golden-style assertions on each error path's first line; no path emits a second dialect.\n\n## Dependencies\n\n- WP-01 (touches the same dispatch/else clauses; sequence after).",
      "preamble": ""
    },
    {
      "seq": 9,
      "title": "Remove test-env branching from lib",
      "scope": "M",
      "status": "done",
      "objective": "Production code must not know it is being tested; the suite must exercise the pipeline production runs. Findings: C6 (13 `Mix.env()` sites across 5 lib files).",
      "body": "## Deliverables\n\n- `settings.all`: delete `build_test_context` fabrication; tests seed real settings through the real path.\n- `main/1`: one display path (the test-only branch merges into the production branch; WP-01's `run/1` largely does this).\n- `load_settings` / `get_setting` / `save_settings`: replace `:test_settings` app-env branching with a config-backed settings source (the test suite points Arca.Config at a temp dir -- `CliCommandHelper.with_clean_config` already provides this pattern).\n- `history_maybe_child_spec` / `config_available?`: replace `Mix.env()==:test` checks with capability checks that hold in all envs. NOTE (found in WP-05): the `Mix.env() == :test && is_pid(Process.whereis(History))` branch never fires. The application starts before `test_helper.exs` runs, so History is not yet registered at that moment and the supervisor is always started -- meaning History IS supervised under test, contrary to the comment above it. Anything relying on that branch is relying on something that does not happen.\n- `Output.test_env?` MIX_ENV env-var check removed (style in tests comes from `ARCA_STYLE=plain`, which the test helper already sets).\n- `namespace_command_helper` / `dev_info` Mix references resolved by WP-06; this WP sweeps the stragglers and adds a CI guard: `grep -c \"Mix.env()\" lib/` must be 0.\n- `lib/arca_cli/testing/cli_fixtures_test.ex` carries 2 of the 13 `Mix.env()` sites -- a test fixture living inside `lib/`. Clean it or relocate it, or the AC-09.1 grep-zero gate cannot pass (vc, 2026-08-04).\n- Full suite migrated and green; no `Application.put_env(:arca_cli, :test_settings, ...)` remains in tests.\n\nNote: `main/1`'s test-only display branch was NOT merged by WP-01 -- WP-01 moved it intact into `run/1`'s `display_response/1` to hold AC-01.4 (display unchanged). Collapsing the two branches is still this WP's job.\n\n## Dependencies\n\n- WP-01 (run/1 collapses main's dual display path); WP-06 (dev.* Mix removal). Highest-risk WP -- the suite currently leans on these branches; budget for test rework. Candidate to defer to 0.5.1 if hv wants to shrink 0.5.0 -- flagged, not assumed.",
      "preamble": ""
    },
    {
      "seq": 10,
      "title": "Docs, changelog, and 0.5.0 release",
      "scope": "S",
      "status": "done",
      "objective": "Ship 0.5.0 with documentation that matches the as-built system and a migration note for every breaking change.",
      "body": "## Deliverables\n\n- CHANGELOG: every WP's user-visible change, breaking changes flagged, deleted-module -> replacement map (WP-07), Laksa-facing note: exit codes now real, `Ctx.complete(:error)` now means what it says.\n- Exit-code contract documented (user guide + reference guide + deployment guide): 0 success/warning, 1 failure; embedding guidance (`run/1` vs `main/1`).\n- Fix doc lies found in audit: BaseCommand snake_case example (`GetDataCommand`/`:get_data`) either supported by WP-06 dispatch work or the doc corrected to the real constraint; BaseSubCommand `@sub_commands` doc; `cli_command_helper` nonexistent-command examples; typos (implementaiton, numver, coammd).\n- VERSION -> 0.5.0; `intent issues close 0001` with Resolutions section pointing at this ST; `intent st` docs updated (`impl.md` as-built notes).\n- Final gate: full suite green; escript smoke run of the E1-E8 probe set from `design.md` with expected-new outcomes recorded in `impl.md`.\n\n## Dependencies\n\n- All prior WPs (WP-09 inclusion per hv scope ruling).",
      "preamble": ""
    },
    {
      "seq": 11,
      "title": "Closing batch: A13 residue, renderer dialect, width pins, changelog gap",
      "scope": "S",
      "status": "done",
      "objective": "Close the last of finding A13 so the release's headline claim is true without a footnote, and close the gaps vc's verification pass found.\n\n0.5.0's headline is that a command's outcome reaches the shell. That claim had four known exceptions when WP-10 shipped: `sys.flush`, `cfg.list`, `BaseSubCommand` and the coordinator all reported failure as ordinary output and exited 0. They were named under \"Known limitations\" rather than shipped silently, pending an hv ruling. hv ruled all fixes into this version, so this WP removes the footnote rather than documenting it better.",
      "body": "## Deliverables\n\n- [x] A18 -- `Arca.Cli.Command.BaseSubCommand.handle/3` returns error tuples. The most important of the four: it is the shared base every subcommand inherits, so a failing subcommand exited 0 in every downstream application too.\n- [x] A19 -- `cfg.list` returns its error tuple. An empty configuration stays a success, matching what `settings.all` already does.\n- [x] A20 -- `Coordinator.inject_subcommands/2` and `update_command_names/3` report a broken command set instead of silently shrinking the CLI.\n- [x] A24 -- `sys.flush` returns its error tuple.\n- [x] Ctx renderer error dialect, per hv's \"both\" ruling: `:ansi` and `:plain` emit `error: <command>: <message>` alongside the `✗` block; `:json` unchanged.\n- [x] A25 (found while doing the above) -- the `:ansi` renderer rendered `ctx.errors` not at all. An error-only context produced the empty string in the one style a human reads.\n- [x] Renderer tests pinned to a fixed `:max_width`, closing vc's N2. Terminal width is no longer an input to the suite.\n- [x] CHANGELOG \"For command authors\" block, closing vc's MED finding from the AC-10.1 reverse walk: six downstream-relevant changes that had no entry.\n- [x] Ledger rows A18, A19, A20, A24, A25, C13; the A24 OPEN row resolved.\n\n## Dependencies\n\n- hv ruling \"all fixes go into this version\", relayed by vc 2026-08-04. Without it the A13 residue stays documented rather than fixed.\n- hv ruling on the Ctx renderer dialect (\"both\": keep the cross-mark presentation and emit a `^error:` line).\n- WP-08's ratified dialect and WP-03's loud `setup/1`, both of which this WP's fixes plug into rather than reinvent.",
      "preamble": ""
    },
    {
      "seq": 12,
      "title": "One predicate for Ctx failure",
      "scope": "S",
      "status": "done",
      "objective": "Make one function the authority on whether a `Ctx` failed, and have every consumer of that question derive from it: the OS exit status, the `error:` dialect line in both text renderers, and the JSON status field.\n\nCloses A28, filed by vc as a MED against WP-11 and ruled in by hv.",
      "body": "## Deliverables\n\n- `Arca.Cli.Ctx.outcome/1` and `Ctx.failed?/1` -- the single authority, holding the rule that was previously private to dispatch: an explicit `complete/2` status wins, and failing that a context carrying errors is a failure.\n- `arca_cli.ex` -- private `ctx_outcome/1` deleted; `process_command_result/3` calls `Ctx.outcome/1`.\n- `plain_renderer.ex` and `ansi_renderer.ex` -- both failure channels (`add_error/2` and `{:error, _}` output items) now render through one `render_failure/2` per renderer. The dialect line is gated on `Ctx.failed?/1`; the `✗` marker is not gated at all.\n- `json_renderer.ex` -- reports `Ctx.outcome/1` rather than the raw `ctx.status` field.\n- `renderer_parity_test.exs` -- the failure cross-product moves from `channel x style` to `channel x completion x style`, asserts a biconditional rather than presence, and gains a literal outcome table plus a JSON agreement product.\n\n## As built\n\nThe unification is byte-identical for every case that was already correct. In both renderers, channel A's failing output (`[error_line, \"\\n✗ \", msg]`) and channel B's (`[error_line, \"\\n\", render_item({:error, msg})]`) expand to the same bytes, because `render_item({:error, msg})` **is** `[\"✗ \", msg]`. So collapsing them onto one function changed nothing except the two rows that were wrong. That is why the suite stayed green at 736 through the source change with zero test edits -- the same signal WP-11 round one gave, and the same meaning: nothing was exercising these paths.\n\nTwo behaviour changes, both driven before and after:\n\n| context                          | before                          | after                          |\n| -------------------------------- | ------------------------------- | ------------------------------ |\n| `add_error \\|> complete(:ok)`    | exit 0, prints `error:`         | exit 0, no `error:`, ✗ kept    |\n| `add_error` with no `complete/2` | exit 1, JSON has no status key  | exit 1, JSON `\"status\":\"error\"` |\n\n`:dump` still shows the raw `ctx.status` including `nil`, deliberately: it is a struct dump for debugging, and misreporting the field as an outcome would defeat its purpose.\n\n## Verification\n\n- 764 green (was 736; +28 net) across seeds 1/3/11/77/555/4242.\n- Widths 40/60/100/200, piped and under a pty.\n- `mix format --check-formatted` and `mix compile --warnings-as-errors` clean, test env included.\n- Escript probe, forced prod rebuild, 12 rows: four success paths exit 0 with zero `^error:`, eight failure paths exit 1 with exactly one. The table carries its own control -- the two groups differ, so the harness is shown to distinguish rather than printing one uniform answer.\n- Both new test groups proven to discriminate by mutation, with the failing set predicted before the run in each case. See AT-12.2 and AT-12.3.\n\n## Dependencies\n\n- Follows WP-11, which vc PASSed. A28 was filed against that PASS and does not reopen it: WP-11's ACs remain satisfied on their own terms, and the AC-11.2 scope-decision note is annotated as superseded rather than rewritten.",
      "preamble": ""
    },
    {
      "seq": 13,
      "title": "Config load diagnosis survives to the user",
      "scope": "S",
      "status": "done",
      "objective": "When configuration cannot be loaded, tell the user why. Closes A29, filed by vc from a cross-repo probe.",
      "body": "## Deliverables\n\n- `cfg_commands.ex` -- `load_settings/0` pattern-matches instead of strict-matching, so the reason from `Arca.Cli.load_settings/0` reaches the user. The bare `rescue` that logged a raw struct and substituted `\"Unknown error loading settings\"` is gone; unexpected exceptions are reported by `execute_command/5`, which already carries their real message. The `@dialyzer {:nowarn_function}` above it went with the strict match that made it necessary.\n- `arca_cli.ex` -- the startup warning in `run/1` carried no reason at all (`\"Error loading settings\"`). It now names it. This is the same defect one level up, and it is what made vc's `about` / `cli.status` observation noisy-but-useless rather than merely noisy.\n- `arca_cli.ex` -- `load_settings/0`'s two messages moved to the ratified dialect: lowercase, and `reason_text/1` in place of `inspect/1` so a binary reason does not arrive wrapped in quotes. The rescue reports `Exception.message/1` rather than `inspect(e)`, so no exception struct reaches a user-facing line.\n- `test/arca_cli/config_diagnosis_test.exs` -- 8 tests driving a real unparseable config through a subprocess.\n\n## As built\n\n**A29 is live on the current pin, which is not how it was filed.** vc found it by building against the unreleased arca_config and reported it as dormant until the dep bump, because the pinned arca_config silently falls back when a config file is MISSING. But a config file that EXISTS and does not parse reaches the identical path on the pinned dependency:\n\n    $ cfg.list        # before, config present and unparseable\n    [error] Error loading settings: %MatchError{term: {:error, \"Failed to load configuration: ...\"}}\n    error: cfg.list: Unknown error loading settings\n\n    $ settings.all    # same trigger, same repo, same underlying call\n    error: settings.all: Failed to load configuration: \"Error parsing config at position: 2, token: ''\"\n\nSo it needed neither the bump nor a local arca_config to reproduce, and it is fixed and verified now rather than prepared for later. hv's single-bump ruling is not engaged: nothing here changes a dependency.\n\n**Making the failure visible is what made its wording matter, for the third time in this thread.** The reason had been reaching a `MatchError` rather than a person, so nobody had read it. Once it reached the dialect line it was capitalised and carried `inspect/1` quotes, both against the ratified form. Same sequence as the coordinator's six messages and History's four in WP-11.\n\n## Verification\n\n- 772 green (was 764) across seeds 1/3/11/77/555/4242; `--check-formatted` and `--warnings-as-errors` clean.\n- Both fixes proven to discriminate by mutation, with the failing set predicted before each run: reverting `cfg_commands.ex` turns exactly 4 red, reverting the startup warning exactly 1.\n- **The first version of these tests was weaker than it looked.** Mutation predicted 4 red and produced 2: the harness row and the two-readers row passed against the reverted fix, because the leaked `%MatchError{}` contains the reason string, so assertions scanning the whole output found it while the user was still being told \"Unknown error loading settings\". They now read the dialect line specifically -- what the user is actually told -- and the mutation produces the predicted 4.\n- vc's harness (`intent/whiteboard/vc/probes/`) run before and after: PASS both times, behaviour probe **identical**. That is a blind spot rather than a reassurance, and it is reported to vc: the harness probes only the missing-config trigger, which this pin falls back on, so it cannot see A29 or its fix. A `BADCFG` row -- config present, unparseable -- would fire on both pins.\n\n## Dependencies\n\n- None. Independent of the arca_config bump in both directions: the defect reproduces without it, and the fix needs nothing from it.",
      "preamble": ""
    },
    {
      "seq": 14,
      "title": "Pin the arca_config contract",
      "scope": "S",
      "status": "done",
      "objective": "State the arca_config surface this CLI depends on, in one place, as a test -- so the dep bump breaks a build rather than someone's terminal. Closes A30 and A31, both found by following up vc's cross-repo observation.",
      "body": "## Deliverables\n\n- `arca_cli.ex` -- `load_settings/0` calls `Arca.Config.reload/0` instead of `Arca.Config.Server.reload/0`. Same delegation target, present in both the pinned and the unreleased arca_config.\n- `cli_command_helper.ex` -- the `@moduledoc` example no longer tells readers to call `Arca.Config.get_config_location/0`, which does not exist in the pinned dependency at all. It uses `Arca.Config.Cfg.config_file/0`, which does exist in both.\n- `test/arca_cli/config_contract_test.exs` -- 8 tests pinning the facade calls, the two unavoidable Server calls, and the liveness probe.\n\n## As built\n\n**A30 -- the hottest path in the CLI was on another repo's internals.** `Arca.Cli.load_settings/0` called `Arca.Config.Server.reload/0`, past the public module. `run/1` loads settings before dispatch for *every* command, so that call is on the path for every invocation. arca_cli tracks arca_config by git branch and every one of these calls resolves at runtime, so a rename there produces no compiler error here -- it produces a crash in a user's terminal after a `mix deps.update`.\n\nThe full executable surface, which turns out to be small:\n\n| call                                  | site                          | status                          |\n| ------------------------------------- | ----------------------------- | ------------------------------- |\n| `Arca.Config.get/1`, `put/2`          | `arca_cli.ex:1075,1156`       | facade, both versions           |\n| `Arca.Config.switch_config_location/1` | `cli_command_helper.ex:500,509` | facade, both versions         |\n| `Arca.Config.Server.reload/0`         | `arca_cli.ex:1032`            | **moved to the facade**         |\n| `Arca.Config.Server.start_link/1`     | `cli_command_helper.ex:549`   | no facade in either -- pinned   |\n| `Arca.Config.Server.delete/1`         | `test_helper.exs:83`          | facade only in local -- pinned  |\n\nvc established that arca_config's own consumer-contract test pins its facade `delete/1` and pins neither `Server.delete/1` nor `Server.start_link/1`, so nothing on that side would notice their removal. That gap is what this WP covers.\n\n**A31 -- a shipped moduledoc documented a function that does not exist.** `cli_command_helper.ex` ships in `lib/`, and its module documentation told readers to call `Arca.Config.get_config_location/0`. That function is absent from the pinned arca_config entirely -- confirmed at runtime, `function_exported?` is false -- and present only in the unreleased one. Anyone following the documented example got an `UndefinedFunctionError`.\n\nI first read this as a live crash on a shipped code path and was wrong: line 350 is inside the `@moduledoc`, which runs to line 375. Checking reachability before reporting is what caught it. The severity is documentation, not runtime.\n\n**The liveness probe is pinned twice, deliberately.** `Arca.Cli.config_available?/0` probes `function_exported?(Arca.Config, :register_change_callback, 2)`. Nothing here calls that function -- WP-07 deleted the callback subsystem -- so a call-graph search from the arca_config side cannot find this consumer, and retiring it there silently flips `config_available?` to false and degrades every `save_settings`. This was a written watch-out on the cc board; it is now a mechanical check.\n\nThe second pin asserts that `config_available?/0` still probes the function this file names. Without it, changing the probe to some other function would leave the existence assertion true of arca_config while no longer describing what this CLI relies on -- a test passing for the wrong reason.\n\n## Verification\n\n- 780 green (was 772) across seeds 1/11/555; `--check-formatted` and `--warnings-as-errors` clean.\n- Proven to discriminate by mutation, failing set predicted first: adding a function arca_config does not export turns exactly 1 red; repointing `@liveness_probe` at a *different real* function also turns exactly 1 red -- and that one matters, because `notify_callbacks/0` does exist, so the existence assertion passed and only the source check caught the drift.\n\n## Dependencies\n\n- None. Both fixes work against the pinned dependency; neither needs the bump.",
      "preamble": ""
    },
    {
      "seq": 15,
      "title": "Absorb the arca_config 0.3.0 error contract",
      "scope": "S",
      "status": "done",
      "objective": "Take the arca_config 0.3.0 dependency bump and absorb its new error contract, so failures read as sentences to a person rather than as Elixir terms. Closes A32, A33 and A34.",
      "body": "## Deliverables\n\n- `arca_cli.ex` `setting_error/2` -- matches `{:config, :not_found, _}` on the atom. The clause that classified a missing key by testing whether the message contained the words \"not found\" is deleted.\n- `arca_cli.ex` `config_reason/1` -- one place that renders an arca_config failure for a person, delegating to `Arca.Config.Error.message/1`. Replaces `reason_text/1`, which did a subset of the same job.\n- `arca_cli.ex` `load_settings/0` -- an absent configuration file reports `{:ok, %{}}` rather than an error, and the config-shaped error is rendered without a second prefix.\n- `config_diagnosis_test.exs` -- a \"states the reason once\" assertion per command.\n\n## As built\n\n**A32 -- arca_config's reasons became tuples, and we printed them at users.** 0.3.0 ratified one error shape, `{:error, {:config, reason, detail}}`, with `Arca.Config.Error.message/1` as the renderer. Anywhere arca_cli interpolated a reason straight into a message now printed a raw term: `settings.get nosuchkey` answered `cannot read setting nosuchkey: {:config, :not_found, [\"nosuchkey\"]}`.\n\nWorth recording that arca_config's own rationale names this repository as the reason the type exists:\n\n> our downstream CLI genuinely does `String.downcase(reason) =~ \"not found\"`, which means rewording a message was a silent breaking change to its behaviour\n\nThat was `arca_cli.ex:1108-1113` and it is now gone, replaced by a match on the atom. `Arca.Config.Cache` still answers a bare `:not_found` deliberately -- \"not cached\" is not the claim \"no such key\" -- so that clause stays.\n\n**A33 -- a fresh install warned on every command.** Before the bump a missing config silently fell back to a different config and reported success; that was a real defect and arca_config's WP-04 removed it. After the bump a missing config reports `:enoent`, which is correct -- but `run/1` loads settings for every command, so every invocation on a fresh install printed a warning about an entirely normal state.\n\nAn absent configuration file is not a failure. A fresh install has no settings yet and \"there are none\" is the honest answer, so `load_settings/0` reports `{:ok, %{}}`. Nothing is swallowed: a config that EXISTS and cannot be parsed still fails loudly carrying its reason, which is the A29 case, and its tests drive it with an unparseable file rather than a missing one.\n\n**A34 -- I introduced this one and the suite could not see it.** `Arca.Config.Error.message/1` renders a complete phrase including its own prefix. Wrapping that in ours printed the sentence twice: `failed to load configuration: failed to load configuration: Error parsing config at position: 2`.\n\nThe A29 tests drive a corrupt config through exactly this path and stayed green, because they assert that the reason *appears* -- and a doubled message still contains the reason. **Third time today an assertion has been satisfied by the malformed output it was meant to reject.** Found by escript probe, not by the suite.\n\n## Verification\n\n- 782 green across seeds 1/11/555 against arca_config head; `--check-formatted` and `--warnings-as-errors` clean; 57/57 AC. Independently reproduced by hv at the same count on the same dep.\n- Escript probe over the three config states: absent (exit 0, silent), missing key (exit 1, one dialect line), unparseable (exit 1, reason stated once).\n- The \"reason once\" tests proven to discriminate: restoring the double prefix turns exactly the two rows red.\n- **The escript is the thing to rebuild.** `cli_debug_persistence_test.exs` drives the built binary, so it kept failing against a stale escript after the source was already correct. The release trap, inside the suite this time.\n\n## Dependencies\n\n- The arca_config 0.3.0 bump. `mix.lock` moved `8b30615` -> `03969fa`. This WP is the only one in ST0011 that depends on it.",
      "preamble": ""
    }
  ],
  "criteria": [
    {
      "id": "AC-00.1",
      "text": "A downstream-style wrapper (Eg.Cli escript pattern) inherits correct exit codes from the dep bump alone: a failing command exits non-zero with no downstream code change",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.1",
      "text": "A failing command run via the built escript exits with status 1; a succeeding command exits 0",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.2",
      "text": "A command that calls `Ctx.complete(:error)` causes `run/1` to return `:error` and the escript to exit 1",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.3",
      "text": "`run/1` never halts the VM; `main/1` halts only for non-`:ok` outcomes",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.4",
      "text": "Success and failure display output is unchanged from 0.4.3 for the existing command set (regression corpus over the E-probe commands)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-01.5",
      "text": "Halt does not truncate piped stdout",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "`cmd | cat` and `cmd > file` transcripts in impl.md"
      }
    },
    {
      "id": "AC-02.1",
      "text": "`--version` prints the name plus the exact content of the VERSION file and exits 0",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-02.2",
      "text": "`about` output contains the VERSION file content and no placeholder strings (\"Arca CLI VERSION\" family)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.1",
      "text": "A configurator declaring `allow_unknown_args: false` and `parse_double_dash: false` reports those values from `config/0` and Optimus enforces them",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.2",
      "text": "A configurator whose setup raises produces a loud failure naming the configurator; the app's command set is never silently replaced by DftConfigurator's",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-03.3",
      "text": "Two configurators registering the same command name resolve to the SAME module in both parse and dispatch (last-registered wins)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.1",
      "text": "A `{:spinner, label, fun}` item's fun executes exactly once per command run under each of ansi, plain, and json styles",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.2",
      "text": "Piped escript output contains no ANSI escape bytes; TTY output may",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.3",
      "text": "Piped escript output preserves UTF-8 content (emoji arrives as its UTF-8 bytes, not `\\x{...}` text)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-04.4",
      "text": "Every `Ctx.output_item` type renders under both ansi and plain (or is explicitly typed as style-specific); `{:list, items}` 2-tuple and `{:json, ...}` included",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-05.1",
      "text": "With the History process down, History client functions return `{:error, :history_not_available, _}` instead of exiting the caller",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-05.2",
      "text": "History never exceeds the configured `history_size` (default 100)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-05.3",
      "text": "`settings.get help_url` is recorded in REPL history; bare `history` is not",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-05.4",
      "text": "`cli.script` fails on an unknown command (no fuzzy rewrite) and stops at the first failure unless `--keep-going`",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.1",
      "text": "`sys.cmd` passes each argument separately (`sys.cmd ls -l -a` succeeds) and prints command output exactly once",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.2",
      "text": "`sys.cmd` propagates the OS exit status (`sys.cmd false` exits non-zero)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.3",
      "text": "`dev.info` and `dev.deps` produce truthful output in the escript: no crash, no fabricated list",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.4",
      "text": "`cli.debug on` persists: a subsequent separate invocation shows debug detail on errors",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.5",
      "text": "A `namespace_command`-generated handle returns the block value (not `[do: value]`) and the module lives under the caller's namespace",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.6",
      "text": "Ctx built by in-repo commands carries `command: <atom>` and a map `args`",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-06.7",
      "text": "No user-supplied string reaches `String.to_atom/1`: repeatedly issuing distinct unknown commands in one REPL session leaves the atom count flat (finding C11; `arca_cli.ex:468`, `help.ex:81,217`, `ctx.ex:377`)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-07.1",
      "text": "Grep-zero across `lib/` for: `load_config_phase`, `Multiplyer`, `err_cloc`, `err_cfloc`, `REPL_MODE`, `is_repl_mode`, `OK.Pipe`; legacy command modules deleted",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-07.2",
      "text": "Pruned deps absent from mix.lock; full suite green after the prune",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-07.3",
      "text": "Changelog maps every deleted public module/function to its replacement",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "CHANGELOG.md 0.5.0 section"
      }
    },
    {
      "id": "AC-08.1",
      "text": "Unknown-command, parse-error, error-tuple, and raised-exception paths all emit the single ratified dialect on their first output line",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-08.2",
      "text": "No user-visible error message renders a plain-string reason inspect-quoted (`\"Key not found\"` class)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-08.3",
      "text": "A command that reports a failure exits non-zero even when it returns its message as a plain string: `settings.get nosuchkey`, `cfg.get nosuchkey` and `cli.redo 999` all exit 1 (finding A13; the `cli.script` and `sys.cmd` legs of the same defect are AC-05.4 and AC-06.2)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-09.1",
      "text": "`grep -r \"Mix.env()\" lib/` returns zero matches",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-09.2",
      "text": "`settings.all` returns real settings under test -- the fabricated test context is deleted",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-09.3",
      "text": "Full suite green with the `:test_settings` app-env mechanism removed",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-09.4",
      "text": "Every documented `expected.out` pattern in the fixture framework actually matches, and still discriminates against non-matching input (finding A14)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-10.1",
      "text": "CHANGELOG covers every WP's user-visible change with breaking flags",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "CHANGELOG.md 0.5.0 section (Three breaking areas flagged (exit codes, stdout/ANSI, removed public names), a replacement map for every removed module and function, an embedder note, and a \"Known limitations\" entry naming the four commands that still exit 0 on failure.)"
      }
    },
    {
      "id": "AC-10.2",
      "text": "Exit-code contract documented in user, reference, and deployment guides",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "doc diffs (User guide: the contract and a shell conditional. Reference guide: `main/1` vs `run/1`, the downstream escript pattern, and a table mapping command return values to exit statuses. Deployment guide: CI usage, stream separation, and an upgrade warning.)"
      }
    },
    {
      "id": "AC-10.3",
      "text": "VERSION reads 0.5.0; issue 0001 closed with a Resolutions section referencing ST0011",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "`intent issues show 0001` (The Resolutions section records the one deliberate departure from the issue's proposed fix (halt in `main/1` rather than at the escript boundary, so downstream inherits the fix from a dependency bump alone) and names the four remaining A13-class instances rather than implying the class is fully closed. Its reproduction claim was re-run against the built escript: exit 1.)"
      }
    },
    {
      "id": "AC-10.4",
      "text": "E1-E8 probe set re-run post-fix with new outcomes recorded",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "impl.md probe table (All eight re-run against the built 0.5.0 escript; table in impl.md.)"
      }
    },
    {
      "id": "AC-11.1",
      "text": "(test) The last four A13-class paths return failure through the outcome channel rather than as display text, **and each of those failure branches is reachable** -- `sys.flush` (A24), `cfg.list` (A19), `Arca.Cli.Command.BaseSubCommand` (A18) and `Arca.Cli.Configurator.Coordinator` (A20, both `inject_subcommands/2` and the silently-skipped command in `update_command_names/3`)",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-11.2",
      "text": "(test) A context that reports failure through **either** of its two channels -- `Ctx.add_error/2` or an `{:error, message}` output item -- emits a line matching `^error:` in the form `error: <command>: <message>` under both text styles, alongside the existing `✗` block; `:json` stays structured and unchanged",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-11.3",
      "text": "(test) Renderer tests do not depend on the width of the terminal that launched them",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-11.4",
      "text": "CHANGELOG documents the six downstream-relevant changes vc's reverse walk found undocumented",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "CHANGELOG.md \"For command authors\" section (Four majors (`execute_command/5` return shape, `BaseSubCommand` error tuples, the `namespace_command` namespace and return-value change, spinner resolution at Ctx build) and two minors (declaration-order argv, exact-match REPL history) in one block addressed to command authors rather than scattered.)"
      }
    },
    {
      "id": "AC-12.1",
      "text": "(test) Exactly one authority decides whether a context failed: `Arca.Cli.Ctx.outcome/1`, with `Ctx.failed?/1` as its boolean reduction. The OS exit status, the `error:` dialect line in both text renderers, and the JSON status field all derive from it, and no site reimplements the rule",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-12.2",
      "text": "(test) In both text styles, a line matching `^error:` appears **if and only if** the context failed, across every combination of failure channel and completion state; the `✗` marker renders unconditionally, so gating the dialect line can never swallow a recorded error",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-12.3",
      "text": "(test) The JSON status field is present for every context and equals `Ctx.outcome/1`, so a machine consumer and the exit status can never disagree",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-13.1",
      "text": "(test) When configuration cannot be loaded, the `error:` line names why, and both commands that read settings give the same reason for the same failure",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-13.2",
      "text": "(test) No exception struct reaches user-facing output on a configuration failure",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-13.3",
      "text": "The messages this makes visible follow the ratified dialect",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "`arca_cli.ex` `load_settings/0`, lowercased, with `reason_text/1` in place of `inspect/1` (A binary reason is already the message and is used as-is, because `inspect/1` wraps it in quotes the dialect does not carry. **Third instance in this thread of unswallowing a failure exposing wording nobody had read**)"
      }
    },
    {
      "id": "AC-14.1",
      "text": "(test) The arca_config surface arca_cli depends on is stated in one place and asserted, so a dependency bump that removes or renames any of it fails a test rather than a user's command",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-14.2",
      "text": "(test) The liveness probe in `config_available?/0` is pinned, and the pin is checked against what the probe actually names",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-14.3",
      "text": "`Arca.Cli.load_settings/0` reaches the config server through the facade, not `Arca.Config.Server.reload/0`",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "`arca_cli.ex` `load_settings/0` ((A30). `run/1` loads settings before dispatch for every command, so that call was on the path for every invocation of this CLI. `Arca.Config.reload/0` delegates to the identical place and exists in both the pinned and the unreleased arca_config, so this is a pure de-coupling with no behaviour change. Two Server calls remain because no facade equivalent exists in the pinned version)"
      }
    },
    {
      "id": "AC-14.4",
      "text": "No shipped documentation instructs a reader to call a function absent from the pinned dependency",
      "kind": "non-test",
      "state": {
        "is": "satisfied",
        "evidence": "`cli_command_helper.ex` `@moduledoc` ((A31). The example called `Arca.Config.get_config_location/0`, which is not defined anywhere in the pinned arca_config (`function_exported?` false at runtime) and exists only in the unreleased one; `cli_command_helper.ex` ships in `lib/`. It now uses `Arca.Config.Cfg.config_file/0`, present in both.)"
      }
    },
    {
      "id": "AC-15.1",
      "text": "(test) No arca_config error term reaches user-facing output as an Elixir value; every one is rendered as a sentence",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-15.2",
      "text": "(test) A fresh install with no configuration file runs silently and successfully; only a configuration that exists and cannot be read fails",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    },
    {
      "id": "AC-15.3",
      "text": "(test) A failure states its reason once",
      "kind": "test",
      "state": {
        "is": "computed"
      }
    }
  ],
  "tests": [
    {
      "id": "AT-00.1",
      "kind": "test",
      "covers": [
        "AC-00.1"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/eg/eg_exit_code_test.exs::\"downstream wrapper exit codes\" (3 tests)"
      }
    },
    {
      "id": "AT-01.1",
      "kind": "test",
      "covers": [
        "AC-01.1"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/exit_code_test.exs::\"failing commands\" + \"succeeding commands\" (8 tests)"
      }
    },
    {
      "id": "AT-01.2",
      "kind": "test",
      "covers": [
        "AC-01.2"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/exit_code_test.exs::\"context status drives exit status\" (2 tests) + test/arca_cli/run_entry_test.exs::\"run/1 outcome\" (7 tests)"
      }
    },
    {
      "id": "AT-01.3",
      "kind": "test",
      "covers": [
        "AC-01.3"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/run_entry_test.exs::\"invariant: a failing run leaves the VM able to run the next command\""
      }
    },
    {
      "id": "AT-01.4",
      "kind": "test",
      "file": "test/arca_cli/display_regression_test.exs",
      "covers": [
        "AC-01.4"
      ],
      "status": "green",
      "note": "(6 tests)"
    },
    {
      "id": "AT-02.1",
      "kind": "test",
      "covers": [
        "AC-02.1"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/version_test.exs::\"--version\" (2 tests)"
      }
    },
    {
      "id": "AT-02.2",
      "kind": "test",
      "covers": [
        "AC-02.2"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/version_test.exs::\"version reporting\" (4 tests)"
      }
    },
    {
      "id": "AT-03.1",
      "kind": "test",
      "covers": [
        "AC-03.1"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/configurator/truthfulness_test.exs::\"explicit false is honoured\" (4 tests)"
      }
    },
    {
      "id": "AT-03.2",
      "kind": "test",
      "covers": [
        "AC-03.2"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/configurator/truthfulness_test.exs::\"broken configurator fails loudly\" (2 tests)"
      }
    },
    {
      "id": "AT-03.3",
      "kind": "test",
      "covers": [
        "AC-03.3"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/configurator/truthfulness_test.exs::\"duplicate command names resolve consistently\" (3 tests)"
      }
    },
    {
      "id": "AT-04.1",
      "kind": "test",
      "file": "test/arca_cli/output/purity_test.exs",
      "covers": [
        "AC-04.1"
      ],
      "status": "green",
      "note": "(7 tests)"
    },
    {
      "id": "AT-04.2",
      "kind": "test",
      "covers": [
        "AC-04.2"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/output/io_correctness_test.exs::\"piped output carries no decoration\" + \"TTY detection\" (3 tests)"
      }
    },
    {
      "id": "AT-04.3",
      "kind": "test",
      "covers": [
        "AC-04.3"
      ],
      "status": "green",
      "legacy": {
        "raw": "test/arca_cli/output/io_correctness_test.exs::\"piped output preserves content\" (2 tests)"
      }
    },
    {
      "id": "AT-04.4",
      "kind": "test",
      "file": "test/arca_cli/output/renderer_parity_test.exs",
      "covers": [
        "AC-04.4"
      ],
      "status": "green",
      "note": "(37 tests)"
    },
    {
      "id": "AT-05.1",
      "kind": "test",
      "file": "test/arca_cli/history/degradation_test.exs",
      "covers": [
        "AC-05.1"
      ],
      "status": "green",
      "note": "(8 tests)"
    },
    {
      "id": "AT-05.2",
      "kind": "test",
      "file": "test/arca_cli/history/bounded_test.exs",
      "covers": [
        "AC-05.2"
      ],
      "status": "green",
      "note": "(5 tests)"
    },
    {
      "id": "AT-05.3",
      "kind": "test",
      "file": "test/arca_cli/repl/should_push_test.exs",
      "covers": [
        "AC-05.3"
      ],
      "status": "green",
      "note": "(8 tests)"
    },
    {
      "id": "AT-05.4",
      "kind": "test",
      "file": "test/arca_cli/commands/cli_script_strict_test.exs",
      "covers": [
        "AC-05.4"
      ],
      "status": "green",
      "note": "(7 tests)"
    },
    {
      "id": "AT-06.1",
      "kind": "test",
      "file": "test/arca_cli/commands/sys_cmd_test.exs",
      "covers": [
        "AC-06.1"
      ],
      "status": "green",
      "note": "(14 tests)"
    },
    {
      "id": "AT-06.2",
      "kind": "test",
      "file": "test/arca_cli/commands/sys_cmd_test.exs",
      "covers": [
        "AC-06.2"
      ],
      "status": "green",
      "note": "(same file)"
    },
    {
      "id": "AT-06.3",
      "kind": "test",
      "file": "test/arca_cli/commands/dev_commands_escript_test.exs",
      "covers": [
        "AC-06.3"
      ],
      "status": "green",
      "note": "(9 tests)"
    },
    {
      "id": "AT-06.4",
      "kind": "test",
      "file": "test/arca_cli/commands/cli_debug_persistence_test.exs",
      "covers": [
        "AC-06.4"
      ],
      "status": "green",
      "note": "(9 tests)"
    },
    {
      "id": "AT-06.5",
      "kind": "test",
      "file": "test/arca_cli/commands/namespace_command_helper_test.exs",
      "covers": [
        "AC-06.5"
      ],
      "status": "green",
      "note": "(8 tests)"
    },
    {
      "id": "AT-06.6",
      "kind": "test",
      "file": "test/arca_cli/ctx_usage_test.exs",
      "covers": [
        "AC-06.6"
      ],
      "status": "green",
      "note": "(9 tests)"
    },
    {
      "id": "AT-06.7",
      "kind": "test",
      "file": "test/arca_cli/atom_safety_test.exs",
      "covers": [
        "AC-06.7"
      ],
      "status": "green",
      "note": "(7 tests)"
    },
    {
      "id": "AT-07.1",
      "kind": "test",
      "file": "test/arca_cli/dead_code_gate_test.exs",
      "covers": [
        "AC-07.1"
      ],
      "status": "green",
      "note": "describe \"purged symbols (AC-07.1)\" (7 tests) -- One test per purged cluster rather than one omnibus grep, so a partial revert names which cluster came back."
    },
    {
      "id": "AT-07.2",
      "kind": "test",
      "file": "test/arca_cli/dead_code_gate_test.exs",
      "covers": [
        "AC-07.2"
      ],
      "status": "green",
      "note": "describe \"dependency prune (AC-07.2)\" (2 tests) -- Split in two because the AC is: absent from `mix.exs` (all ten), and absent from `mix.lock` (only the three with no other dependant). See the AC-07.2 amendment above."
    },
    {
      "id": "AT-07.3",
      "kind": "test",
      "covers": [
        "AC-07.1"
      ],
      "status": "green",
      "note": "It covers the GATE itself and is AC-07.1's positive control rather than another instance of it: without this row, a scanner that silently matched nothing would report every invariant above as satisfied.",
      "legacy": {
        "raw": "test/arca_cli/dead_code_gate_test.exs::\"invariant: the scanner finds strings that are genuinely present\""
      }
    },
    {
      "id": "AT-08.1",
      "kind": "test",
      "file": "test/arca_cli/error_format_test.exs",
      "covers": [
        "AC-08.1"
      ],
      "status": "green",
      "note": "(24 tests)"
    },
    {
      "id": "AT-08.2",
      "kind": "test",
      "file": "test/arca_cli/error_format_test.exs",
      "covers": [
        "AC-08.2"
      ],
      "status": "green",
      "note": "(same file, \"reasons are text\" describe)"
    },
    {
      "id": "AT-08.3",
      "kind": "test",
      "file": "test/arca_cli/exit_code_test.exs",
      "covers": [
        "AC-08.3"
      ],
      "status": "green",
      "note": "(18 tests, \"commands that report failure as a plain string\" describe)"
    },
    {
      "id": "AT-09.1",
      "kind": "test",
      "file": "test/arca_cli/no_test_env_gate_test.exs",
      "covers": [
        "AC-09.1"
      ],
      "status": "green",
      "note": "(4 tests) -- Proven to discriminate: a temporary `Mix.env()` added to `output.ex` turned it red naming `lib/arca_cli/output.ex:217`, and removing it turned it green. Carries its own control test, so a scanner that silently matched nothing could not report the invariant as satisfied."
    },
    {
      "id": "AT-09.2",
      "kind": "test",
      "covers": [
        "AC-09.2"
      ],
      "status": "green",
      "note": "Written in place of the planned new file: the existing test was the defect, accepting any of three outputs including the fabricated one, so replacing it removes the false green rather than leaving it beside a new test.",
      "legacy": {
        "raw": "test/arca_cli/cli/arca_cli_test.exs::\"settings.all reports the settings actually in force\""
      }
    },
    {
      "id": "AT-09.3",
      "kind": "test",
      "covers": [
        "AC-09.3"
      ],
      "status": "green",
      "note": "717 green, 10 consecutive runs (6 piped, 4 pty). -- The AC has two halves and they need different kinds of evidence: that the mechanism is gone is a property of the source, asserted by this test; that the suite is green without it is the run itself",
      "legacy": {
        "raw": "test/arca_cli/no_test_env_gate_test.exs::\"invariant: settings come from configuration, not from application env\""
      }
    },
    {
      "id": "AT-09.4",
      "kind": "test",
      "file": "test/arca_cli/testing/cli_fixtures_pattern_test.exs",
      "covers": [
        "AC-09.4"
      ],
      "status": "green",
      "note": "(9 tests) -- (fixed early, in WP-02, because it blocked the version fixture)"
    },
    {
      "id": "AT-11.1",
      "kind": "test",
      "covers": [
        "AC-11.1"
      ],
      "status": "green",
      "note": "that the original config is NOT returned as though injection had succeeded. -- Behavioural, not textual: a malformed args map drives the real `BaseSubCommand` failure branch, and a probe module whose `config/0` returns the wrong shape drives both coordinator branches. The A20 tests assert the negative that matters",
      "legacy": {
        "raw": "test/arca_cli/outcome_channel_test.exs, describe \"A18: the shared subcommand base\" (2 tests) + describe \"A20: the configurator coordinator\" (5 tests)"
      }
    },
    {
      "id": "AT-11.2",
      "kind": "test",
      "covers": [
        "AC-11.1"
      ],
      "status": "green",
      "note": "Carries its own control test, so a scanner that silently matched nothing could not report the invariant as satisfied. What it proves is bounded and stated in the file: these four constructs cannot return, not that a new one cannot appear.",
      "legacy": {
        "raw": "test/arca_cli/outcome_channel_test.exs, describe \"gate: the failure-as-display-text constructs stay gone\" (2 tests)"
      }
    },
    {
      "id": "AT-11.3",
      "kind": "test",
      "file": "test/arca_cli/output/ansi_renderer_test.exs",
      "covers": [
        "AC-11.2"
      ],
      "status": "green",
      "note": "describe \"render/1 with context errors\" (5 tests) -- The first is a regression test for A25 stated as its own assertion (`refute render(ctx) == \"\"`), separate from the dialect assertions, so a future change that renders *something* wrong is distinguishable from one that renders nothing."
    },
    {
      "id": "AT-11.4",
      "kind": "test",
      "covers": [
        "AC-11.2"
      ],
      "status": "green",
      "note": "The grep invariant is asserted against ANSI-stripped output in the ansi test and raw output in the plain test, because greppability is the actual requirement and escape codes would defeat it.",
      "legacy": {
        "raw": "test/arca_cli/output/plain_renderer_test.exs::\"the dialect line names the command when the context has one\" + ::\"the dialect line starts a line, so `grep '^error:'` finds it\""
      }
    },
    {
      "id": "AT-11.5",
      "kind": "test",
      "file": "test/arca_cli/output/plain_renderer_test.exs",
      "covers": [
        "AC-11.3"
      ],
      "status": "green",
      "note": "describe \"render_item/1 - table width\" (2 tests) -- Asserts the renderer copes at 40 columns and that a wide width keeps every column on one line, which is the property the five pinned order tests depend on."
    },
    {
      "id": "AT-11.6",
      "kind": "test",
      "file": "test/arca_cli/history/degradation_test.exs",
      "covers": [
        "AC-11.1"
      ],
      "status": "green",
      "note": "describe \"sys.flush with History down\" (3 tests) -- Lives beside the unregister seam that makes it possible rather than with the other A13 coverage, because a failure branch is only covered if something can drive it. Added after vc proved the first A24 fix inert."
    },
    {
      "id": "AT-11.7",
      "kind": "test",
      "file": "test/arca_cli/output/renderer_parity_test.exs",
      "covers": [
        "AC-11.2"
      ],
      "status": "green",
      "note": "the `@failure_channels` x `@text_styles` cross-product (4 tests) + describe \"the dialect line means failure, not decoration\" (1 test) -- Written as a cross-product rather than as separate assertions because the completeness claim about this dialect has now been wrong twice, and both times the untested *combination* was the broken one. A third failure channel added later needs a row in `@failure_channels` and fails until it has one."
    },
    {
      "id": "AT-12.1",
      "kind": "test",
      "file": "test/arca_cli/output/renderer_parity_test.exs",
      "covers": [
        "AC-12.1"
      ],
      "status": "green",
      "note": "the `@outcome_table` rows (8 tests) -- This table exists because AT-12.2 compares the renderers against `Ctx.outcome/1`, which proves the four sites AGREE but cannot prove the authority is right: both sides would move together. The expected outcomes here are literals, so a change to `Ctx.outcome/1` has to be argued for in this table rather than silently ratified by the tests that depend on it."
    },
    {
      "id": "AT-12.2",
      "kind": "test",
      "file": "test/arca_cli/output/renderer_parity_test.exs",
      "covers": [
        "AC-12.2"
      ],
      "status": "green",
      "note": "the axis that discriminated was the one held constant, and A28 walked straight through it. Each test asserts the biconditional and, separately, that the failure text survived. -- the `@failure_channels` x `@completions` x `@text_styles` cross-product (16 tests) -- Replaces AT-11.7's `channel x style` product, which pinned `complete(:error)` on every row"
    },
    {
      "id": "AT-12.3",
      "kind": "test",
      "file": "test/arca_cli/output/renderer_parity_test.exs",
      "covers": [
        "AC-12.3"
      ],
      "status": "green",
      "note": "describe \"the JSON status is the same authority as the exit status\" (8 tests) -- Asserts against `Ctx.outcome/1` for every channel x completion pair, which is what catches the dropped-key case: a never-completed failing context decoded to `nil` before the fix, because the raw status field was `nil` and the nil-rejection removed it from the document."
    },
    {
      "id": "AT-13.1",
      "kind": "test",
      "file": "test/arca_cli/config_diagnosis_test.exs",
      "covers": [
        "AC-13.1"
      ],
      "status": "green",
      "note": "describe \"a load failure reports its reason (A29)\" -- Drives a real unparseable config file through a subprocess rather than mocking the config server, which is possible because the corrupt-file trigger works on the pinned dependency."
    },
    {
      "id": "AT-13.2",
      "kind": "test",
      "file": "test/arca_cli/config_diagnosis_test.exs",
      "covers": [
        "AC-13.2"
      ],
      "status": "green",
      "note": "the per-command \"leaks no exception struct at the user\" tests (2) -- Matches any `%Module{` shape rather than `%MatchError{` specifically, so a different leaked struct fails too."
    },
    {
      "id": "AT-13.3",
      "kind": "test",
      "file": "test/arca_cli/config_diagnosis_test.exs",
      "covers": [
        "AC-13.1"
      ],
      "status": "green",
      "note": "describe \"a command that does not read configuration\" (2 tests) -- Asserts both halves of the startup-warning behaviour: a config-independent command still exits 0 with a broken config, and the warning it emits names the reason."
    },
    {
      "id": "AT-13.4",
      "kind": "test",
      "covers": [
        "AC-13.1"
      ],
      "status": "green",
      "note": "`config/dotenv.exs` calls `System.put_env/2` under `:dev` and `:test`, overwriting `ARCA_CLI_CONFIG_PATH` from the parent environment, so a child cannot be steered by exporting it. The seam sets it inside the evaluated code, after config evaluation. Without this test a child that never reached the broken config would produce plausible-looking failures.",
      "legacy": {
        "raw": "test/arca_cli/config_diagnosis_test.exs::\"invariant: the broken config is the one the CLI reads\""
      }
    },
    {
      "id": "AT-14.1",
      "kind": "test",
      "file": "test/arca_cli/config_contract_test.exs",
      "covers": [
        "AC-14.1"
      ],
      "status": "green",
      "note": "describe \"the facade surface arca_cli calls\" (4 tests) -- One test per call site rather than one omnibus assertion, so a bump names which function went rather than reporting that something did."
    },
    {
      "id": "AT-14.2",
      "kind": "test",
      "file": "test/arca_cli/config_contract_test.exs",
      "covers": [
        "AC-14.1",
        "AC-14.3"
      ],
      "status": "green",
      "note": "describe \"the Server internals arca_cli reaches for\" (2 tests) -- Held separate from the facade list so that reaching past the public module stays visible as an exception rather than becoming precedent. vc established that arca_config's own contract test pins neither of these."
    },
    {
      "id": "AT-14.3",
      "kind": "test",
      "file": "test/arca_cli/config_contract_test.exs",
      "covers": [
        "AC-14.2"
      ],
      "status": "green",
      "note": "describe \"the liveness probe\" (2 tests) -- Two assertions, not one: that the probed function exists, and that `config_available?/0` still probes the function this file pins. Without the second, changing the probe would leave the first true of arca_config while no longer describing anything this CLI relies on."
    },
    {
      "id": "AT-15.1",
      "kind": "test",
      "covers": [
        "AC-15.1"
      ],
      "status": "green",
      "note": "Not a new test: it is the existing dialect assertion, which went red at the bump naming the raw tuple it had started printing. A test that already asserted the right thing and simply began failing is the best possible evidence that the contract changed.",
      "legacy": {
        "raw": "test/arca_cli/error_format_test.exs::\"failure: a setting that does not exist\""
      }
    },
    {
      "id": "AT-15.2",
      "kind": "test",
      "covers": [
        "AC-15.2"
      ],
      "status": "green",
      "note": "the release trap, inside the suite. -- Also pre-existing, also went red at the bump. It drives the built escript, which is why it kept failing after the source was already correct",
      "legacy": {
        "raw": "test/arca_cli/commands/cli_debug_persistence_test.exs::\"invariant: a fresh install starts with debug off\""
      }
    },
    {
      "id": "AT-15.3",
      "kind": "test",
      "file": "test/arca_cli/config_diagnosis_test.exs",
      "covers": [
        "AC-15.3"
      ],
      "status": "green",
      "note": "the per-command \"states the reason once, not twice\" tests (2) -- Proven to discriminate: restoring the double prefix turns exactly those two rows red and no others."
    }
  ],
  "attachments": [
    {
      "path": "design.md",
      "text": "# Design - ST0011: Fable review of arca_cli base code\n\n## Approach\n\nFull-codebase audit of `lib/` (all 55 files read), triggered by issue 0001 (every command exits 0). The exit-code bug destroyed information mid-pipeline; the audit swept for the same failure family everywhere: information destroyed in flight, dead branches, environment-dependent behaviour, duplicated concerns, and API that does not do what it documents.\n\nEvery finding below is verified against the as-built code at `ca7ba57`, not the narrative. Runtime findings carry a probe reference: P-probes ran under `mix run` against the compiled library; E-probes ran against a freshly built escript. Baseline before any change: 493 tests passing (31 doctests, 462 tests), 0 failures. `mix compile --force` emits no warnings.\n\nVerdict: the codebase is functionally rich but systematically loses correctness information at boundaries. Three loss archetypes recur: outcomes discarded (exit codes, Ctx.status, sys.cmd status), configuration read but never honoured (allow_unknown_args, history_size, debug_mode persistence), and behaviour that silently depends on environment (renderer side effects by TTY, test-only code paths, escript-only crashes). A 0.5.0 breaking release should fix the archetypes, not just the instances.\n\n## Findings A: Confirmed correctness failures\n\nAll confirmed by probe. Severity: C = breaks correctness for users/automation, V = visibly wrong output.\n\n| ID  | Sev | Finding                                                                  | Evidence                                                                                         | Probe   |\n| --- | --- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------ | ------- |\n| A1  | C   | Every command exits 0; outcome destroyed at 3 points                     | `lib/arca_cli.ex:321` hardcoded `:ok`; `:739` drops `ctx.status`; `:654-667` stringifies errors  | 8-cmd repro table, all exit=0 |\n| A2  | C   | `--version` prints the help screen, not a version                        | no `:version` clause in `handle_args/3`; USAGE advertises the flag                                | E1      |\n| A3  | V   | Three version sources disagree; `about` reports 0.1.0, real is 0.4.3     | `config/config.exs` `version: \"0.1.0\"`; `VERSION` 0.4.3; `dft_configurator.ex:36` \"Arca CLI VERSION\" | P8, E2  |\n| A4  | C   | `allow_unknown_args: false` and `parse_double_dash: false` silently coerced to `true` | `base_configurator.ex:107-108` `\\|\\| true`; the identical bug was fixed for `sorted` at `:110-116` | P3 -> `{true, true, false}` |\n| A5  | C   | History \"graceful degradation\" is illusory: `rescue` cannot catch GenServer exits | `history.ex:100-108` (+4 siblings); same pattern `cli_status_command.ex:57-64`, `sys_flush_command.ex:52-63` | P1 -> `{:caught, :exit, :noproc...}` |\n| A6  | C   | Renderers execute business logic only in ANSI mode: `{:spinner, label, fun}` runs `fun` on TTY, silently skips it otherwise | `ansi_renderer.ex:251-261,282-298` executes; `plain_renderer.ex:156-161`, `json_renderer.ex:95-101` label-only | P5      |\n| A7  | C   | Scripts and redo run through REPL fuzzy matching: a typo silently executes a different command | `cli_script_command.ex:205,218` -> `Repl.eval_for_redo` -> `repl.ex:597` `try_fuzzy_match`        | E8: script line `abut` ran `about` |\n| A8  | C   | `dev.info` crashes in escript (`Mix.env` undefined); `dev.deps` prints a fabricated hardcoded dep list in escript | `dev_info_command.ex:16`; `dev_deps_command.ex:55-71` (wrong versions, missing real deps)         | E4, E5  |\n| A9  | C   | `cli.debug on` persistence is inert: setting saved, nothing loads it at boot; next invocation has debug off while `cli.debug` reports ON | `cli_debug_command.ex:54-62`; no boot reader of `\"debug_mode\"` (grep)                             | E7      |\n| A10 | C   | `sys.cmd`: output printed twice (2nd time as inspected tuple), multiple args joined into ONE OS arg, OS exit status printed bare then discarded | `sys_cmd_command.ex:47-51,54-68`; duplicate print via `utils.ex:257` `put_lines(tuple)->IO.inspect` | `echo hi` -> `hi` + `{\"hi\\n\", 0}`; `ls -l -a` -> usage error; `false` -> cli exit 0 |\n| A11 | C   | Ctx's own consumers hold it wrong: command atom passed as `args`, `ctx.command` stays nil, `ctx.args` is an atom not a map | `settings_all_command.ex:46,55,80`, `cli_history_command.ex:17`, `sys_info_command.ex:25` vs `ctx.ex:122-130` | P2 -> `{nil, :\"sys.info\"}` |\n| A12 | V   | Piped output mangles unicode (emoji -> literal `\\x{1F4E6}`) while ANSI colour codes leak INTO pipes (forced `ansi_enabled`) | escript io encoding unset; `arca_cli.ex:249` `Application.put_env(:elixir, :ansi_enabled, true)` unconditional | E3 hexdump; E4/E8 `[31m`/`[36m` in piped output |\n\n| A13 | C   | Leaf commands discard their own outcome: a failure returned as a plain string is indistinguishable from success, so the command exits 0 even with the exit-code plumbing in place | `settings_get_command.ex:55-65` returns `message` on `{:error, message}`; `cli_script_command.ex` and `cli_redo_command.ex` do the same | escript after WP-01: `settings.get nosuchkey`, `cfg.get nosuchkey`, `cli.script /nonexistent`, `cli.redo 999` all still exit 0 |\n\n| A14 | C   | Two of the fixture framework's five documented `expected.out` patterns never matched anything: the replacement keys are ordinary double-quoted strings, where `\"\\d\"` is Elixir's DEL escape (0x7F) and `\"\\w\"` silently drops the backslash | `cli_fixtures_test.ex:548-549` -- `String.replace(\"{{\\d+}}\", ...)` builds the key `{{<DEL>+}}`, which no fixture file can contain | `byte_size(\"{{\\d+}}\") == 6`, and `\"{{\\w+}}\" == \"{{w+}}\"` |\n\n### A-finding mechanics worth recording\n\n- A14 detail: found in WP-02 when a fixture was rewritten to use `{{\\d+}}` so it would stop breaking on every version bump. The pattern silently fell through to literal comparison, so the fixture failed however correct the output was -- the same \"documented behaviour quietly does not happen\" family as A9's inert `cli.debug`. Fixed with `~S` sigils on both keys; all five documented patterns now have positive and negative test cases. Assigned AC-09.4 (WP-09 owns that file), but fixed on the spot in WP-02 because it blocked the version fixture.\n- A13 detail: found by re-probing the repro set after WP-01 landed. A1 was the plumbing (outcome destroyed in transit); A13 is the same archetype one layer down (outcome never created). WP-01 fixed 7 of the 11 repro commands; the remaining 4 are commands that pattern-match their own failure and then flatten it to a display string, which `process_command_result/3` can only read as success. The `cli.script` leg is already AC-05.4 and the `sys.cmd` leg AC-06.2; `settings.get` / `cfg.get` / `cli.redo` had no home, so they are now AC-08.3 -- WP-08 is where the error dialect changes anyway, so fixing them there changes the display once rather than twice. Fixing them inside WP-01 would have violated the ratified AC-01.4 (display unchanged).\n- A1 detail: `main/1:294-311` error branches are dead code -- `handle_subcommand/4` and `handle_args/3` stringify all errors upstream, so tuples can never reach them. `parse_command_line/3` returns a plain String for every failure mode (probed). `Ctx.complete/2` is documented, public, stores the status, and exactly one debug renderer ever reads it.\n- A2 detail: Optimus's `:version` parse result falls through `handle_args`'s `_other ->` catch-all into `generate_filtered_help`. Even if routed, the Optimus version string is the placeholder \"Arca CLI VERSION\".\n- A5 detail: `GenServer.call` to a dead/absent process raises an exit signal, not an exception. Every `try/rescue` wrapper in `history.ex` (get_state, push_cmd, get_history_length, get_history, flush_history) misses it. The REPL prompt path (`repl_prompt -> History.hlen`) therefore crashes rather than degrading if History dies.\n- A6 detail: this makes command behaviour depend on output style. The same Ctx does its work on a TTY and silently does nothing under `ARCA_STYLE=plain`, in tests, in pipes, and in JSON mode. Renderers must be pure; execution belongs in the command layer.\n- A12 detail: the two axes are inverted. Content (emoji) that should survive a pipe is destroyed; decoration (ANSI codes) that should be stripped in a pipe is forced on. `Output`'s careful TTY detection exists but is bypassed by the forced flag and by the legacy string path.\n\n## Findings B: Dead machinery (grep-confirmed, zero callers)\n\n| ID  | Finding                                                                                     | Evidence                                                                    |\n| --- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |\n| B1  | Entire config-callback subsystem unreachable: `load_config_phase` has no caller (mix.exs has no `start_phases`), so `register_config_callbacks`, `handle_config_change`, `apply_display_settings`, subscriptions never run | `arca_cli.ex:150-235,1064-1097`                                              |\n| B2  | Hardcoded foreign-project reference: `Multiplyer.Config.Server`                              | `arca_cli.ex:1056,1081`                                                      |\n| B3  | Seven unregistered legacy command modules shipped as public API: Flush, Get, History, Redo, Status, Sys, Settings (+ example SubCommand/OneCommand); three generations of the same commands (bare / sys.* / cli.*, cfg.*, settings.*); `SysCommand` is a literal copy-paste of `SysCmdCommand` | `dft_configurator.ex:8-32` registers none of them                            |\n| B4  | ErrorHandler location macros never used: `create_error_with_location`, `create_and_format_error_with_location`, `err_cloc`, `err_cfloc`, `__using__` | `error_handler.ex:18-32,342-429`; grep: zero call sites                      |\n| B5  | `REPL_MODE` env var consumed in two places, set by nothing; the file-logging feature it gates is unreachable without an undocumented manual export | `arca_cli.ex:112`, `config/runtime.exs`                                      |\n| B6  | `Process.put(:is_repl_mode, true)` written, never read                                       | `repl.ex:64`                                                                 |\n| B7  | `use OK.Pipe` in two modules, zero `~>>` pipelines anywhere; `:ok` dep likely droppable      | `arca_cli.ex:85`, `utils.ex:18`                                              |\n| B8  | Six unused runtime deps shipped to every consumer: `table_rex`, `pathex`, `elixir_uuid`, `ucwidth`, `certifi`, `castore` (only textual mention is dev.deps' fabricated list); `dotenv` dep unused beside the hand-rolled parser in `config/dotenv.exs` | grep across `lib/`                                                           |\n| B9  | HTTP-client residue in Utils: `form_encoded_body` (does NOT URL-encode despite the name), `parse_json_body`, `fetch_body`, `decode_json` -- no callers in `lib/`, tests only | `utils.ex:50-113`                                                            |\n| B10 | Commented-out blocks and vestigial params: `check_initialization_status`, `intro/2` ignoring both parameters | `arca_cli.ex:256-257,325-341,1214-1218`                                      |\n\n## Findings C: Design debt and API traps\n\n| ID  | Finding                                                                                    | Evidence                                                                        |\n| --- | ------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------- |\n| C1  | `namespace_command` macro generates `handle/3` returning `[do: result]` -- the keyword wrapper leaks into output; masked in tests by `=~` substring assertions. Also generates modules into `Arca.Cli.Commands.*` for EVERY consuming app -> cross-app collision | `namespace_command_helper.ex:84-87,102-104`; P4 -> `[do: \"pong-payload\"]`         |\n| C2  | Dispatch resolves by module-name reconstruction, not registration: snake_case command names are impossible (BaseCommand's own doc example `GetDataCommand`/`:get_data` fails its own validation); hand-rolled CommandBehaviour modules bypass validation and become registered-but-undispatchable; name collisions resolve first-module-wins while Optimus config resolves last-wins -- parse and dispatch can disagree | `arca_cli.ex:554-578`; `base_command.ex:145-146,184-238`; `coordinator.ex:394-397` |\n| C3  | Coordinator swallows configurator failure and silently replaces the app's ENTIRE command set with arca defaults | `coordinator.ex:73-77`                                                            |\n| C4  | `BaseSubCommand.extract_arguments` rebuilds positional argv from `Map.values` order (term order, not declaration order) -- latent arg-reorder for multi-arg subcommands; moduledoc shows `@sub_commands` attribute style that the implementation ignores (reads config key) | `base_sub_command.ex:20-24,57,100-107,253-256`                                    |\n| C5  | Error-format Highlander violation: 3+ user-visible dialects -- `\"Error (type): msg\"` (ErrorHandler), `\"error: prefix: msg\"` (Cli.handle_error family), raw unprefixed strings (commands returning error text), plus REPL's deprecated formatter set | `error_handler.ex:159`; `arca_cli.ex:891-907`; `repl.ex:173-181`                  |\n| C6  | 13 `Mix.env()` sites in lib across 5 files; `settings.all` fabricates fake data under test; `main/1` displays through a test-only path; the suite partially exercises a pipeline production never runs. `Output.test_env?` checks the `MIX_ENV` OS var, which `mix test` does not set -- unreliable duplicate of the same idea | `settings_all_command.ex:42-49,68-85`; `arca_cli.ex:277-281,1035-1037,1117-1126,1175-1179`; `output.ex:160-165` |\n| C7  | Style detection triplicated and disagreeing: `Output.determine_style` (TERM + ansi_enabled) vs `AnsiRenderer.check_tty` (TERM only) vs forced `ansi_enabled` in `main/1` | `output.ex:82-175`; `ansi_renderer.ex:58-64`; `arca_cli.ex:249`                   |\n| C8  | `should_push?` uses substring matching: any command whose text CONTAINS history/redo/flush/help is silently excluded from REPL history (eg `settings.get help_url`) | `repl.ex:830,852`                                                                 |\n| C9  | Dead knobs cluster: `history_size` default defined, never enforced (history unbounded); `debug_mode` persisted, never loaded (A9); `Ctx.status` stored, never acted on (A1) | `arca_cli.ex:1152`; `history.ex` (no trim)                                        |\n| C10 | mix.exs junk keys silently ignored: `mix_tasks:` is not a Mix project key; `ansi_enabled:` is not an application key | `mix.exs:18-21,27-29`                                                             |\n| C11 | `String.to_atom` on user input in help paths (7 sites) -- unbounded atom creation in a long REPL session | `arca_cli.ex:468`; `help.ex:81,217`; `ctx.ex:377` -- assigned to WP-06 as AC-06.7 (vc flagged it as the one confirmed finding without a home, 2026-08-04) |\n| C12 | `put_lines(map/tuple) -> IO.inspect` as user-facing output (how sys.cmd's tuple reaches the screen) | `utils.ex:256-257`                                                                |\n| C13 | Ansi/Plain renderer duplication (~120 lines of table-option wrangling + `remove_header_lines` + `safe_to_string`), already drifted: `{:list, items}` 2-tuple renders in ansi, silently dropped in plain; `{:json, ...}` items supported by all renderers but absent from `Ctx.output_item` type | `ansi_renderer.ex:113-247,416-441` vs `plain_renderer.ex:146-177,443-468`; `ctx.ex:65-74` |\n| C14 | Latent REPL crashes: `repl/3` else lacks a 3-tuple error clause (WithClauseError if one ever arrives); `eval_for_redo` matches only 4-tuple errors | `repl.ex:139-158,964-969`                                                         |\n| C15 | Minor hygiene: `Ctx.add_output` O(n) list append per item; `Help.normalize_app_name` `with` that can never fail (and would map a tuple into a line list if it did); inspect-wrapped values in user output (`\"Key not found\"` with quotes); doc typos (implementaiton, numver, coammd); `cli_command_helper` docs reference nonexistent `config.set` command | `ctx.ex:150-152`; `help.ex:267-303`; `arca_cli.ex:1135`                           |\n\n## Design Decisions\n\nThe remediation is organised into 10 work packages (WP01-WP10, elaborated in `WP/` and sequenced in `tasks.md`). Ratified by hv 2026-08-04 together with the 7 open decisions tabulated in `tasks.md`; `acceptance.md` is RATIFIED. Key design rulings the fixes follow:\n\n1. One status pipeline (WP01): commands report outcomes; the dispatcher carries `{status, output}`; string-returning legacy functions become thin adapters (Highlander-compliant). `System.halt` lives only at the process entry boundary (`main/1`); `run/1` is the pure embeddable API. Downstream inherits correct exit codes from a dep bump with zero code changes.\n2. One version source (WP02): the `VERSION` file. App env, configurator default, and Optimus version all derive from it.\n3. Config must be honoured or rejected, never coerced (WP03): explicit `false` survives; a broken configurator is a loud startup error, not a silent command-set swap.\n4. Renderers are pure (WP04): rendering never executes command logic. Spinner/progress semantics move to the command layer; one style detector; ANSI decided by TTY, not forced; escript io set to unicode.\n5. Interactive conveniences stay interactive (WP05): fuzzy matching prompts in the REPL only; scripts and redo are strict.\n6. No test-only code paths in lib (WP09): the suite must exercise the production pipeline.\n\n## Alternatives Considered\n\n- Fix only issue 0001 and ship 0.4.4: rejected -- the same information-loss archetype recurs in at least 11 other confirmed places; a breaking 0.5.0 that fixes the archetypes is materially better and amortises one migration cost.\n- Separate halting entry module (`Arca.Cli.CLI.main/1`) leaving `main/1` pure: rejected in favour of `main/1` halting + new pure `run/1`, because every downstream escript already points at `main/1`; the alternative forces all consumers to edit their escript config to get the fix.\n- Deprecate rather than delete dead machinery (B1-B10): deletion chosen for a 0.x breaking release; modules with plausible downstream use (legacy command modules, Utils HTTP residue) get a deprecation note in the changelog listing replacements.\n\n## Probe Appendix\n\nRuntime probes (`mix run`, scratchpad `probe2.exs`):\n\n| Probe | Question                                         | Result                                                        |\n| ----- | ------------------------------------------------ | ------------------------------------------------------------- |\n| P1    | Does History's rescue catch a dead-process call? | No -- `{:caught, :exit, {:noproc, ...}}` escaped the rescue    |\n| P2    | Does Ctx.new misuse swap fields?                 | Yes -- `{command, args} = {nil, :\"sys.info\"}`                  |\n| P3    | Are explicit `false` config values honoured?     | No -- `{allow_unknown_args, parse_double_dash, sorted} = {true, true, false}` |\n| P4    | What does a namespace_command handle return?     | `[do: \"pong-payload\"]`                                         |\n| P5    | Does spinner fun run in plain? in ansi?          | plain: `:not_ran`; ansi: `:ran`                                |\n| P6    | Does fuzzy match rewrite typos?                  | `fuzzy_match(\"abut\") = {:single, \"about\"}`                     |\n| P7    | decode_json with map opts                        | Works (`{:ok, %{a: 1}}`) -- no finding, withdrawn              |\n| P8    | App-env version vs VERSION file                  | `{\"0.1.0\", \"0.4.3\"}`                                           |\n\nEscript probes (`_build/escript/arca_cli`):\n\n| Probe | Command                                  | Result                                                                   |\n| ----- | ---------------------------------------- | ------------------------------------------------------------------------ |\n| E1    | `--version`                              | Prints full help screen                                                  |\n| E2/E3 | `about` piped + hexdump                  | Literal bytes `\\x{1F4E6}` instead of emoji; reports version 0.1.0        |\n| E4    | `dev.info`                               | UndefinedFunctionError Mix.env, red ANSI stacktrace leaked into pipe     |\n| E5    | `dev.deps`                               | Fabricated hardcoded list (wrong optimus version, missing real deps)     |\n| E6    | `sys.cmd echo hi` / `ls -l -a` / `false` | `hi` then `{\"hi\\n\", 0}` double print; joined `\"-l -a\"` breaks ls; OS exit 1 -> CLI exit 0 |\n| E7    | `cli.debug on` then new invocation       | Reports ON, behaves OFF                                                  |\n| E8    | script containing `abut`                 | Fuzzy-matched and executed `about`, debug log leaked in colour           |\n\nExit-code repro (all exit 0): `about` (correct), `settings.get nosuchkey`, `cfg.get nosuchkey`, `cli.error standard|raise|legacy`, `cli.script /nonexistent`, unknown command.\n",
      "bytes": 21498,
      "sha256": "7082e182cce3f9c90436bdd18bfb7bb1a88a2a4502d99ae394ba16ac1e76da31"
    },
    {
      "path": "impl.md",
      "text": "# Implementation - ST0011: Fable review of arca_cli base code\n\n## Implementation\n\nStatus: WP-01 complete. WP-02..WP-10 not started.\n\n### WP-01 as-built -- exit codes (issue 0001)\n\nThe shape is a status-carrying core with display-only adapters over it, so nothing downstream or in the REPL had to change:\n\n- Every function named `dispatch*` returns `{outcome, output}`: `dispatch/3`, `dispatch_args/3`, `dispatch_subcommand/4`, `dispatch_command_help/2`. `outcome` is `:ok | :error | :warning`.\n- `parse_command_line/3`, `handle_args/3`, `handle_subcommand/4` and `handle_command_help/2` are now one-line adapters (`dispatch*(...) |> elem(1)`) keeping their historical string-returning contracts. The REPL calls `handle_args/3` and was untouched.\n- `execute_command/5` returns `{:ok, outcome, output}` on success (error tuples unchanged). This is a breaking change to a public function -- it needs a CHANGELOG entry in WP-10.\n- `process_command_result/3` folds the context status in, making `Ctx.complete/2` load-bearing for the first time. `ctx_outcome/1` takes an explicit status when set, and otherwise treats a context carrying errors as a failure rather than a success.\n- `Arca.Cli.run/1` is the new pure entry point: parses, dispatches and displays exactly as before, returns the outcome, never halts.\n- `Arca.Cli.main/1` is now `argv |> run() |> halt_for()`. `halt_for/1` halts with 1 on `:error` only; `:ok` and `:warning` return normally so the escript exits 0 through normal shutdown. This satisfies AC-01.3 (\"halts only for non-`:ok`\") while leaving the success path byte-identical for embedders.\n- The dead error branches at `arca_cli.ex:294-311` are deleted, as is the duplicated USAGE-line rewriting (extracted to `rewrite_usage_app_name/2`).\n- `cli.error` gained `ctx` and `warning` modes so the context status channel is exerciseable from the shell, not just from tests.\n- `lib/arca_cli/testing/cli_command_helper.ex` ran commands through `main/1`; switched to `run/1`, since it executes inside the host test VM.\n- 22 in-repo test call sites migrated from `main/1` to `run/1`. `Eg.Cli` keeps `main/1 -> Arca.Cli.main/1` (it documents the downstream escript pattern) and gained a `run/1` passthrough for in-VM tests.\n\nVerification: 520 tests pass (was 493 at `ca7ba57`), no compile warnings under `--warnings-as-errors`. 7 of the 11 repro commands now exit correctly; the remaining 4 are finding A13, assigned to AC-08.3 / AC-05.4 / AC-06.2.\n\n### WP-02 as-built -- version truth\n\n- `VERSION` is the single source. `config/config.exs`'s hardcoded `version: \"0.1.0\"` is deleted; `Arca.Cli.version/0` reads an app-configured `:version` when one exists and otherwise resolves `Application.spec(:arca_cli, :vsn)`, which mix generates from `VERSION`. No hardcoded fallback -- a string that can drift is worse than none.\n- `BaseConfigurator` resolves an undeclared `:version` from the app spec at runtime via the new `app_version/1`, so a downstream configurator reports its own app's version rather than a placeholder.\n- `DftConfigurator`'s four placeholder strings are gone; branding now comes from app config via `Application.compile_env/3`, and `:version` is deliberately not declared so the app-spec default applies.\n- `dispatch_args/3` gained a `:version` clause. Optimus reports `--version` as a bare atom, which previously fell through the catch-all into the help screen.\n- Three tests and one golden fixture asserted `arca_cli 0.1.0` -- they were pinning the defect. They now read `VERSION` at compile time (the fixture uses the framework's digit pattern), so a release bump does not falsify them.\n\n### WP-03 as-built -- configurator truthfulness\n\n- The `|| true` coercions are gone. Defaults now live in exactly one place: `config/2` records only what a configurator actually declared, and `__before_compile__` supplies defaults through `attribute_or_default/3`. Previously both sites carried defaults, which is what made an explicit `false` indistinguishable from unset.\n- Unrecognised configurator options now raise a compile-time `IO.warn` instead of being silently dropped, so a typo cannot quietly disable an option.\n- `Coordinator.setup/1` raises instead of falling back to `DftConfigurator`. The old fallback replaced the app's entire command set on any configurator error, producing a CLI that ran and did the wrong thing rather than one that refused to start.\n- `handler_for_command/2` searches from the end of the command list, so dispatch resolves a duplicate command name to the same registration Optimus's last-wins merge picks. Parse and dispatch can no longer disagree about what a command means.\n- `update_command_names/3` now appends rather than prepends. The accumulated list is read as registration order when reporting a duplicate, and prepending made its last element the *first* registration -- the \"last registered wins\" warning was naming the wrong module until this was fixed.\n\n### WP-04 as-built -- pure renderers and correct io\n\n- Spinner/progress purity (A6): `Ctx.add_output/2` now runs a `{:spinner, label, fun}`'s function when the item is added and stores `{:spinner, label, result}`. Renderers only draw. `Output.render/1` also calls the new `Ctx.resolve_output/1` as a safety net, so a context built as a struct literal can never hand a function to a renderer either. Resolving twice is a no-op, since a resolved item no longer holds a function.\n- Plain and JSON renderers now show the spinner's *result*, not just its label -- previously the information existed only in ANSI output.\n- One style detector (A12): `AnsiRenderer.check_tty/1` is deleted. It was a second, differently-written TTY check (TERM only) that could disagree with `Output.determine_style/1`, which is now the sole authority.\n- Escript unicode (A12): `configure_io/0` sets `:io.setopts(:standard_io, encoding: :unicode)`. An escript's stdio defaults to latin1, which rendered any codepoint above 255 as literal `\\x{...}` text.\n- ANSI in pipes (A12): the fix was to *remove* `Application.put_env(:elixir, :ansi_enabled, true)`, not to replace it. The runtime already determines at boot whether stdout is a terminal and sets that flag correctly; the bug was overriding it unconditionally.\n- `Output.tty?/0` is public and asks `:prim_tty.isatty(:stdout)`. Two rejected alternatives are recorded in its docstring: `TERM` is inherited by piped processes and so describes the launching terminal rather than the destination, and `:io.columns/1` reports `:enotsup` under `-noinput` even on a real terminal, which would have disabled colour everywhere.\n- Renderer parity (AC-04.4): `{:list, items}` as a 2-tuple was ANSI-only and fell through the plain renderer's catch-all to `nil`; `{:json, ...}` was implemented in both renderers but missing from the `output_item` type. Both fixed, and a parity test now walks every item type against every style.\n\nFive pre-existing renderer tests asserted the defect directly (\"renders spinner with function execution\"). They now assert the new contract: renderers receive resolved items and execute nothing.\n\n### WP-05 as-built -- History and REPL integrity\n\n- A5, real exit handling: `GenServer.call/2` to a dead or absent process *exits* the caller rather than raising, so the `try/rescue` in all five History clients could never catch it -- the documented graceful degradation never happened and the caller died instead. Since the REPL prompt asks for the history length on every redraw, a History crash took the whole session with it. All five now go through one private `call/3` using `catch :exit`, replacing five copies of an ineffective wrapper with a single working one.\n- A5 cost: five near-identical try/rescue blocks collapsed into one helper -- the duplication is what let all five be wrong in the same way without anyone noticing.\n- AC-05.2, bounded history: `history_size` (default 100, `config :arca_cli, history_size: n`) is enforced on push. The index is now tracked in state as `next_index` rather than derived from list length -- once history is full the length stops growing, so derived indices would repeat and `cli.redo` could no longer tell entries apart.\n- AC-05.3, exact exclusion: `should_push?/1` matched the exclusion list with `=~`, dropping any command that merely *contained* `history`, `redo`, `flush` or `help`. `settings.get help_url` was never recorded. Now matches the first token exactly. The list form also joined arguments with `\"\"` rather than `\" \"`, which manufactured matches out of adjacent words.\n- A7 and the A13 `cli.script` leg: scripts and redo now use the new `Repl.eval_strict/3` -- exact command names, no fuzzy matching, no history push, and it returns `{outcome, output}`. Fuzzy matching stays a convenience of the interactive prompt, where a human can see the substitution. `cli.script` stops at the first failure unless `--keep-going`, and returns an error tuple so the outcome reaches the exit status.\n- Escript re-probe: a script containing `abut` now prints `error: Unknown command: abut` and exits 1, where it previously ran `about` and exited 0. A missing script file exits 1.\n\nThree pre-existing tests changed. Two asserted the old behaviour directly (a script continuing past failures, an unreadable file returning a display string); the third compared the whole History struct and so was disturbed by the new `next_index` field -- it now asserts the history itself.\n\n### A14, found during WP-02\n\nRewriting the `about` golden fixture to use `{{\\d+}}` (so it would stop breaking on version bumps) exposed that the pattern never worked. The replacement keys in `cli_fixtures_test.ex` were ordinary double-quoted strings, where `\"\\d\"` is Elixir's DEL escape (0x7F) and `\"\\w\"` silently drops the backslash -- so `String.replace` was looking for keys no fixture file could contain, and both patterns fell through to literal comparison. Verified directly: `byte_size(\"{{\\d+}}\") == 6`, and `\"{{\\w+}}\" == \"{{w+}}\"`. Fixed with `~S` sigils; all five documented patterns now carry positive and negative cases. Same family as A9's inert `cli.debug`: documented behaviour that quietly does not happen.\n\nSession 2026-08-04 (cc):\n\n- Read all 55 files in `lib/` at `ca7ba57`. Catalogued findings in `design.md` as A (confirmed correctness failures), B (dead machinery), C (design debt).\n- Verified findings by probe rather than by inspection alone -- 8 runtime probes under `mix run`, 8 escript probes against a freshly built `_build/escript/arca_cli`. Probe transcripts are in `design.md`'s Probe Appendix.\n- One candidate finding was withdrawn after probing (P7: `Utils.decode_json` with map options works correctly).\n- Elaborated WP-01..WP-10 with objectives, deliverables and a dependency DAG (`tasks.md`).\n- Drafted the acceptance contract; hv ratified it in full along with all 7 open decisions on 2026-08-04.\n\n## Code Examples\n\nThe halt boundary, kept as small as it can be so that everything above it stays pure and testable:\n\n```elixir\n@spec main([String.t()]) :: :ok | no_return()\ndef main(argv) do\n  argv\n  |> run()\n  |> halt_for()\nend\n\n@spec halt_for(outcome()) :: :ok | no_return()\ndefp halt_for(:error), do: System.halt(1)\ndefp halt_for(outcome) when outcome in [:ok, :warning], do: :ok\n```\n\nThe adapter pattern that let 462 existing tests and the REPL keep working while the outcome channel was threaded underneath them:\n\n```elixir\n# Canonical: carries the outcome\n@spec dispatch_subcommand(atom(), map(), map(), term()) :: dispatch_result()\ndef dispatch_subcommand(cmd, args, settings, optimus) do\n  with {:ok, handler} <- find_command_handler(cmd),\n       {:ok, outcome, result} <- execute_command(cmd, args, settings, optimus, handler) do\n    {outcome, result}\n  else\n    {:error, error_type, reason, debug_info} ->\n      {:error, handle_error({:error, error_type, reason, debug_info})}\n\n    {:error, error_type, reason} ->\n      {:error, handle_error({:error, error_type, reason})}\n  end\nend\n\n# Adapter: the historical display-only contract\ndef handle_subcommand(cmd, args, settings, optimus) do\n  dispatch_subcommand(cmd, args, settings, optimus) |> elem(1)\nend\n```\n\nMaking `Ctx.complete/2` load-bearing, with a no-silent-errors fallback for contexts that carry errors but never called it:\n\n```elixir\n@spec ctx_outcome(Ctx.t()) :: outcome()\ndefp ctx_outcome(%Ctx{status: status}) when status in [:ok, :error, :warning], do: status\ndefp ctx_outcome(%Ctx{errors: [_ | _]}), do: :error\ndefp ctx_outcome(%Ctx{}), do: :ok\n```\n\n## Technical Details\n\nConstraints discovered during the audit that bind the implementation:\n\n- `handle_args/3` is shared by the one-shot dispatch path AND the REPL (`repl/repl.ex:599,615`). Its string-returning contract must survive as an adapter over any new status-carrying implementation (WP-01), or the REPL breaks.\n- ~20 in-repo test call sites invoke `Arca.Cli.main/1` inside `capture_io`. These must migrate to the new pure `run/1` BEFORE `main/1` gains `System.halt`, or `mix test` kills its own VM.\n- Downstream projects wrap rather than call directly: `Eg.Cli.main/1 -> Arca.Cli.main/1`, with the downstream project's own `escript: [main_module: ...]` (`test/arca_cli/eg/eg_cli_test.exs:14`). Laksa follows the same pattern. Halting must therefore live in `main/1` so downstream inherits correct exit codes from a dep bump alone.\n- `main/1:294-311` error branches are unreachable: `handle_subcommand/4` and `handle_args/3` stringify every error upstream. Probed -- `parse_command_line/3` returns a plain String for every failure mode tested.\n- The test suite currently depends on lib's `Mix.env()` branches (fabricated `settings.all` data, test-only display path in `main/1`, `:test_settings` app-env store). WP-09's purge therefore carries real test-rework cost; it is deliberately sequenced late.\n\n## Challenges & Solutions\n\n| Challenge                                                             | Resolution                                                                                     |\n| --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |\n| Distinguishing real defects from plausible-looking ones                | Every A-finding backed by a runtime or escript probe; one candidate withdrawn when it passed     |\n| Adding an outcome channel without breaking the REPL or 462 tests       | Status-carrying implementation + one-line string-returning adapters (Highlander-compliant)       |\n| Making exit codes work downstream without forcing consumer code edits  | `main/1` halts (downstream already points at it); new pure `run/1` for tests and embedding       |\n| zsh does not word-split unquoted vars -- first repro run was misleading | Re-ran with a shell function taking `\"$@\"`; corrected results recorded in the probe appendix     |\n| Proving \"display unchanged\" rather than asserting it                    | Built a git worktree at `ca7ba57`, ran the same corpus through both checkouts, diffed (WP-01)    |\n| The same zsh trap recurred while building that corpus harness           | Rewrote the harness as a bash script with `read -r -a`; the zsh run had passed multi-word commands as one argv entry |\n| Test suite killed its own VM the moment `main/1` gained `System.halt`   | Predicted on the board; migrated 22 call sites to `run/1`, and the in-lib test helper with them  |\n\n## AC-01.5 evidence -- halt does not truncate piped stdout\n\n`System.halt/1` flushes pending IO before exiting (it is `:erlang.halt/1`, whose default options enable flushing), so no flush-safe variant was needed. Measured two ways.\n\nVolume probe -- 50,000 lines then halt, under each redirection mode:\n\n| Mode              | Lines expected | Lines observed | Exit |\n| ----------------- | -------------- | -------------- | ---- |\n| `cmd \\| wc -l`    | 50000          | 50000          | 0    |\n| `cmd \\| wc -l` (halt 1) | 50000    | 50000          | 1    |\n| `cmd > file`      | 50000          | 50000          | 1    |\n\nReal escript, comparing byte counts against the unredirected run:\n\n| Command                              | Bytes | Exit |\n| ------------------------------------ | ----- | ---- |\n| `arca_cli --help` (direct)           | 1025  | 0    |\n| `arca_cli --help \\| cat`             | 1025  | 0    |\n| `arca_cli --help > file`             | 1025  | 0    |\n| `arca_cli cli.error standard \\| cat` | 62    | 1    |\n| `arca_cli cli.error standard > file` | 62    | 1    |\n\nNo truncation in any mode, including the paths that halt with a non-zero status.\n\n## AC-01.4 evidence -- display unchanged from 0.4.3\n\nA worktree at `ca7ba57` and the current tree were each run over a 17-command corpus (the E-probe repro set plus the common success paths), same config dir, `NO_COLOR=1`, MIX_ENV=dev. The captures were diffed.\n\nResult: zero differences in command output. The only diffs were the `exit=` markers, plus timestamps and stack-frame line numbers inside the `Logger.error` diagnostic that `cli.error raise` writes (an internal log line, not command output).\n\nExit-code deltas, all in the correct direction:\n\n| Command                  | 0.4.3 | 0.5.0-dev | Verdict                    |\n| ------------------------ | ----- | --------- | -------------------------- |\n| `about`                  | 0     | 0         | unchanged                  |\n| `--help`                 | 0     | 0         | unchanged                  |\n| `help settings.all`      | 0     | 0         | unchanged                  |\n| `settings.all`           | 0     | 0         | unchanged                  |\n| `cli.error success`      | 0     | 0         | unchanged                  |\n| `sys.info`               | 0     | 0         | unchanged                  |\n| `help`                   | 0     | 1         | fixed (invalid subcommand) |\n| `settings.get`           | 0     | 1         | fixed (missing argument)   |\n| `cli.error standard`     | 0     | 1         | fixed                      |\n| `cli.error legacy`       | 0     | 1         | fixed                      |\n| `cli.error raise`        | 0     | 1         | fixed                      |\n| `nosuchcommand`          | 0     | 1         | fixed                      |\n| `help nosuchcommand`     | 0     | 1         | fixed                      |\n| `settings.get nosuchkey` | 0     | 0         | finding A13 -> AC-08.3     |\n| `cfg.get nosuchkey`      | 0     | 0         | finding A13 -> AC-08.3     |\n| `cli.redo 999`           | 0     | 0         | finding A13 -> AC-08.3     |\n| `cli.script /nonexistent`| 0     | 0         | finding A13 -> AC-05.4     |\n\n## AC-01.4 re-baseline -- WP-06\n\nPer the ratified clarification, the display corpus re-baselines at each WP whose ACs change output, and the diff is recorded here. WP-06 is such a WP: AC-06.1, AC-06.2 and AC-06.3 all require different output from 0.4.3.\n\nA worktree at `828e20f` (the WP-05 close) and the current tree were each run over a 26-command corpus -- the previous 17 plus the WP-06 targets -- with the same config dir, `NO_COLOR=1`, MIX_ENV=dev, and the captures diffed after normalising log timestamps, stack-frame line numbers and the embedded version string.\n\nResult: 18 of 26 command blocks byte-identical; 8 changed, every one of them a WP-06 target.\n\n| Command                 | Changed | Why                                                                    |\n| ----------------------- | ------- | ---------------------------------------------------------------------- |\n| `about`                 | no      |                                                                        |\n| `help`                  | no      |                                                                        |\n| `help settings.all`     | no      |                                                                        |\n| `settings.all`          | no      |                                                                        |\n| `settings.get`          | no      |                                                                        |\n| `settings.get nosuchkey`| no      |                                                                        |\n| `cfg.get nosuchkey`     | no      |                                                                        |\n| `cli.error standard`    | no      |                                                                        |\n| `cli.error legacy`      | no      |                                                                        |\n| `cli.error raise`       | no      |                                                                        |\n| `cli.error success`     | no      |                                                                        |\n| `cli.script /nonexistent`| no     |                                                                        |\n| `cli.redo 999`          | no      |                                                                        |\n| `nosuchcommand`         | no      |                                                                        |\n| `help nosuchcommand`    | no      |                                                                        |\n| `sys.info`              | no      |                                                                        |\n| `cli.debug`             | no      |                                                                        |\n| `cli.history`           | no      |                                                                        |\n| `--help`                | yes     | one line: `dev.deps` about text now describes what it reports (AC-06.3) |\n| `dev.info`              | yes     | table of facts readable in every deployment format (AC-06.3)           |\n| `dev.deps`              | yes     | real loaded applications, not the mix dependency list (AC-06.3)        |\n| `sys.cmd echo hello`    | yes     | the `{output, status}` tuple no longer printed after the output (AC-06.1) |\n| `sys.cmd expr 1 + 1`    | yes     | `2` instead of `1 + 1` -- arguments no longer joined (AC-06.1)         |\n| `sys.cmd false`         | yes     | names the exit status, exit 0 -> 1 (AC-06.2)                           |\n| `sys.cmd nosuchbinary`  | yes     | one clean error, exit 0 -> 1 (AC-06.1, AC-06.2)                        |\n| `sys.cmd`               | yes     | \"no OS command given\" instead of a `KeyError` stack trace (A15)        |\n\nA note on the harness, because it cost time and will cost it again. `config/dotenv.exs` loads `config/.env`, which is gitignored, and that file sets `ARCA_CLI_CONFIG_PATH`. Being loaded during config evaluation, it overrides any value exported by the shell -- so the corpus harness's config isolation silently does not apply, and a fresh `git worktree` (which has no `config/.env`) resolves a different config path than the working tree. The first run of this comparison therefore showed a spurious `settings.all` difference. Both checkouts need the same `config/.env` for the comparison to mean anything.\n\n## Escript gate -- WP-06\n\nRebuilt and re-run over the smoke set, from a scratch working directory so settings writes stay out of the repository:\n\n| Command                | Exit | First line                                    |\n| ---------------------- | ---- | --------------------------------------------- |\n| `sys.cmd echo hello`   | 0    | `hello`                                       |\n| `sys.cmd expr 1 + 1`   | 0    | `2`                                           |\n| `sys.cmd ls -l -a`     | 0    | listing includes dotfiles, so `-a` took effect |\n| `sys.cmd false`        | 1    | `✗ false exited with status 1`                |\n| `sys.cmd nosuchbinary` | 1    | `✗ command not found: nosuchbinary`           |\n| `sys.cmd`              | 1    | `✗ no OS command given`                       |\n| `dev.info`             | 0    | reports `Deployment: escript`, no crash       |\n| `dev.deps`             | 0    | reports `arca_config 0.2.0`, absent from the fabricated list |\n| `cli.debug on`         | 0    | `Debug mode is now ON`                        |\n| `cli.debug` (next run) | 0    | `Debug mode is currently ON`                  |\n\n## AC-01.4 re-baseline -- WP-08\n\nWP-08 changes error text by construction: AC-08.1 ratifies one dialect and AC-08.3 changes three commands from printing a message to reporting a failure. Corpus re-run against `65d253a` (the WP-06 close), 28 commands, same method as before.\n\nResult: 17 blocks byte-identical, 11 changed -- every one of them an error path. No success path moved.\n\n| Command                  | Was                                                              | Now                                                        |\n| ------------------------ | ---------------------------------------------------------------- | ---------------------------------------------------------- |\n| `nosuchcommand`          | `error: Unknown command: nosuchcommand`                          | `error: nosuchcommand: unknown command`                    |\n| `sys`                    | `error: Unknown command: sys`                                    | + namespace listing (A16: previously unreachable)          |\n| `sys.inf`                | `error: Unknown command: sys.inf`                                | + \"Did you mean\" block (A16)                               |\n| `help nosuchcommand`     | `error: unknown command: nosuchcommand`                          | `error: nosuchcommand: unknown command`                    |\n| `settings.get nosuchkey` | `Failed to get setting nosuchkey: \"Key not found\"`               | `error: settings.get: setting not found: nosuchkey`        |\n| `cfg.get nosuchkey`      | `Failed to get setting nosuchkey: \"Key not found\"`               | `error: cfg.get: setting not found: nosuchkey`             |\n| `cli.redo 999`           | `error: invalid command index: 999`                              | `error: cli.redo: no command at history index 999`         |\n| `cli.error standard`     | `Error (invalid_argument): ...`                                  | `error: cli.error: ...`                                    |\n| `cli.error legacy`       | `Error (command_failed): ...`                                    | `error: cli.error: ...`                                    |\n| `cli.error raise`        | `Error (command_failed): Error executing command cli.error: ...` | `error: cli.error: ...`                                    |\n| `cli.script /nonexistent`| `Error (script_not_readable): ...`                               | `error: cli.script: ...`                                   |\n\nTwo changes in that table are worth calling out because neither was in the plan.\n\nThe `sys` and `sys.inf` rows are finding A16. The suggestion machinery -- `find_similar_commands/1`, the \"Did you mean\" block, the namespace listing -- was unreachable. The unknown-command path called `handle_error/1` with a list, which takes the list clause; only the string clause consults the suggestions. The feature had unit tests and had never fired for a user. Routing the path through `handle_error(cmd, \"unknown command\", :command_not_found)` was needed for the dialect anyway, and lit it up as a side effect.\n\nThe `cli.error raise` row is smaller than it looks and larger than it looks. The dialect line changed once; but the `Logger.error` diagnostic that preceded it was on **stdout**, so the first line of a failed command was a stack trace, and a caller piping the CLI got log lines mixed into the data. That is finding A17, and it is the same defect family as A12: pipes must carry content, not diagnostics. Logger is now configured to stderr, which is what makes AC-08.1's \"first output line\" true for this path.\n\nEvidence, from a scratch working directory, on the built escript:\n\n    $ arca_cli cli.error raise 2>/dev/null\n    error: cli.error: This is a test exception from CliErrorCommand\n    $ arca_cli cli.error raise 2>&1 >/dev/null | head -1\n    14:41:09.661 [error] Error executing command cli.error: %RuntimeError{...}\n\nMoving Logger to stderr made log output visible in the test run, because ExUnit's stdout capture no longer intercepts it. `ExUnit.start(capture_log: true)` is the right answer: it holds log output per test and prints it only for a failing one.\n\n## AC-08.3 evidence -- finding A13 closed\n\nThe three commands named in the AC, plus the two legs closed earlier, all exit non-zero at a real process boundary:\n\n| Command                   | 0.4.3 | WP-08 | Leg closed in |\n| ------------------------- | ----- | ----- | -------------- |\n| `settings.get nosuchkey`  | 0     | 1     | WP-08          |\n| `cfg.get nosuchkey`       | 0     | 1     | WP-08          |\n| `cli.redo 999`            | 0     | 1     | WP-08          |\n| `cli.script /nonexistent` | 0     | 1     | WP-05          |\n| `sys.cmd false`           | 0     | 1     | WP-06          |\n\nThe shared root was one habit, not three bugs: a command that had already turned its failure into a display string had nothing left to report. Fixing the dispatch plumbing in WP-01 could not help, because there was no outcome to propagate.\n\n## WP-09 as-built -- the environment branches, and what was hiding behind them\n\nTwelve sites, removed in six steps with the suite green between each, so a red suite could only ever mean the step just taken.\n\n| Site                                          | What it did                                       | Disposition                                    |\n| --------------------------------------------- | ------------------------------------------------- | ---------------------------------------------- |\n| `arca_cli.ex` `history_maybe_child_spec`      | skipped the supervisor when History was up        | deleted -- never fired (proven in WP-05)       |\n| `arca_cli.ex` `config_available?`             | answered `true` under test without looking        | deleted -- other two branches were identical   |\n| `arca_cli.ex` commented `check_initialization` | nothing                                           | deleted                                        |\n| `arca_cli.ex` `display_response`              | printed everything under test                     | collapsed onto the production branch           |\n| `arca_cli.ex` `load_settings`/`get`/`save`    | read and wrote `:test_settings` app env           | deleted -- settings go through Arca.Config     |\n| `settings_all_command.ex` (x2)                | answered a fabricated \"test/true\" table           | deleted with `build_test_context`              |\n| `output.ex` `test_env?`                       | forced plain when `MIX_ENV=test`                  | deleted -- never fired (A21)                   |\n| `cli_fixtures_test.ex` (x2)                   | refused to eval `setup.exs` outside `:test`       | asks ExUnit whether a run is in progress        |\n\nThe ordering was the whole risk. Every one of these deletions is safe only once the test suite has a real configuration to read, so making the isolation genuine came first and nothing was deleted until it did. Steps 1 and 2 -- the branches proven dead -- then landed with **no test changes at all**, which is what makes \"these never fired\" a measurement rather than a claim.\n\n### What the branches were concealing\n\nEach removal exposed something the branch had been standing in front of.\n\n- `config_available?` had three branches where two were byte-identical and the third guessed. Once the guess was gone, the capability check had to actually hold under test -- probed first, and it does: module loaded, function exported, server running.\n- `display_response`'s test branch printed `{:nooutput, _}` responses, which production skips. That is `cli.script`'s success return, so the suite had been watching a display path no user has ever seen.\n- Deleting `Output.test_env?` left one test (\"uses plain style in test environment\") passing for an entirely different reason -- TTY detection rather than the deleted rule -- which would have failed under a pty. A test that keeps passing after you delete what it covers is not evidence of anything.\n\n### The isolation that was not\n\n`Arca.Config.Cfg.config_pathname/0` resolves `ARCA_CLI_CONFIG_PATH` before `ARCA_CONFIG_PATH`, and `config/.env` sets the former. Every test setup in the suite set the latter. The block also wrote its config file into a directory that does not exist and dropped the `{:error, :enoent}`, and restored itself with `System.put_env(map)`, which cannot delete a variable that was previously unset. It appeared in nine files and did nothing in all nine.\n\nIt is now done once, in `test_helper.exs`, with `Arca.Config.switch_config_location/1` -- which re-points the already-booted server *and* sets the app-specific variables, so subprocesses inherit the same location instead of reaching for the repository's tracked config.\n\n### The subprocess harness (A23)\n\n`mix test` does not export `MIX_ENV`. The children therefore ran in `:dev` against `_build/dev` -- and with `_build/dev` moved aside, **5 of 16 failed**. The shape of that failure is the point:\n\n| Assertion class      | Child cannot start | Reads as        |\n| -------------------- | ------------------ | --------------- |\n| `assert exit == 1`   | still passes       | correct failure |\n| `assert exit == 0`   | fails              | CLI defect      |\n\nSo a broken harness looks like a product bug, and half the suite goes on passing for a reason unrelated to the CLI. The three files now share one runner (`test/support/cli_subprocess.ex`) that pins `MIX_ENV`, scrubs `ARCA_STYLE`/`NO_COLOR` so ambient state cannot decide what a child prints, and fails loudly with \"harness failure\" when the child never reached the CLI. One test asserts the child's `--version` output exactly, so the harness itself is covered rather than assumed.\n\nMix writes its build-lock notice to **stdout**, interleaved with the child's real output, which made that new assertion intermittently fail. Worth stating plainly: the lock contention was never only cosmetic, and the previous silence was bought by running children against a build nobody had compiled. The runner strips that one line and nothing else.\n\n## As-built probe re-run (post-fix) -- AC-10.4\n\nThe E1-E8 set from `design.md`, re-run against the built 0.5.0 escript. Same probes, same order, run from a scratch directory with its own config so nothing in the repository is read or written.\n\n| Probe | 0.4.3                                                              | 0.5.0                                                             |\n| ----- | ------------------------------------------------------------------ | ----------------------------------------------------------------- |\n| E1    | `--version` printed the whole help screen                          | `arca_cli 0.5.0`, exit 0                                          |\n| E2    | `about` reported version 0.1.0                                     | reports 0.5.0, matching VERSION and the app spec                   |\n| E3    | piped emoji arrived as the literal text `\\x{1F4E6}`                | arrives as its UTF-8 bytes `f0 9f 93 a6`; 0 ESC bytes in the pipe |\n| E4    | `dev.info` raised UndefinedFunctionError, red ANSI into the pipe    | renders its table, exit 0                                         |\n| E5    | `dev.deps` printed a fabricated list with wrong versions           | reports what is loaded: jason 1.4.5, optimus 0.5.1, owl 0.13.1     |\n| E6    | `sys.cmd echo hi` printed `hi` then `{\"hi\\n\", 0}`; `false` exited 0 | raw output is exactly `hi\\n` (3 bytes); `false` exits 1            |\n| E7    | `cli.debug on` reported ON while behaving OFF                      | survives the process boundary: ON, shows debug detail, OFF sticks  |\n| E8    | script line `abut` silently ran `about`                            | `error: abut: unknown command`, script stops, exit 1               |\n\nE6 is the one worth reading twice. The old double-print was a tuple leaking to the user; the fix is not that the tuple is now formatted nicely, it is that there is nothing extra to format. `xxd` on the piped output shows three bytes.\n\n### Release-process trap found here\n\nBumping `VERSION` alone does not change the built version. `mix.exs` reads it with `File.read!(\"VERSION\")` at project-load time, but Mix does not track `VERSION` as an input to `mix.exs`, so nothing looks stale and no recompile is triggered. The first probe run reported `arca_cli 0.4.3` from an escript built after the bump.\n\n`touch mix.exs && mix compile --force` before `mix escript.build`. This is a real way to ship a release whose binary reports the previous version, which is the same class of defect as A3 -- a version that is true in one place and stale in another.\n\n## WP-11 escript probe -- both failure channels, both outcomes\n\nRun against the built escript from a scratch directory with its own config, after the A26 and A27 fixes. The `^error:` column is the count of lines matching `^error:` in combined stdout+stderr, which is the property AC-11.2 actually claims.\n\n| Command                 | exit | `^error:` | first line                                          |\n| ----------------------- | ---- | --------- | --------------------------------------------------- |\n| `about`                 | 0    | 0         | `📦 Arca CLI`                                        |\n| `sys.flush`             | 0    | 0         | `Command history cleared successfully`               |\n| `cfg.list`              | 0    | 0         | `Configuration Settings:`                            |\n| `cli.error success`     | 0    | 0         | `Success: No error occurred`                         |\n| `cli.error warning`     | 0    | 0         | `⚠ This is a context warning test`                   |\n| `cli.error ctx`         | 1    | 1         | `error: cli.error: This is a context error test`     |\n| `cli.error standard`    | 1    | 1         | `error: cli.error: This is a standard error tuple test` |\n| `cli.error legacy`      | 1    | 1         | `error: cli.error: This is a legacy error tuple test` |\n| `sys.cmd false`         | 1    | 1         | `error: sys.cmd: false exited with status 1`         |\n| `cfg.get nosuchkey_xyz` | 1    | 1         | `error: cfg.get: setting not found: nosuchkey_xyz`   |\n| `nosuchcommand`         | 1    | 1         | `error: nosuchcommand: unknown command`              |\n\n`cli.error ctx` is the row that matters: it reports through `Ctx.add_output({:error, _})` rather than `Ctx.add_error/2`, and before A27 it exited 1 with **zero** matches. `cli.error warning` is the negative control -- a warning is a success carrying notes, so it must stay at exit 0 with no dialect line.\n\n### The first run of this table was worthless and looked perfect\n\nWritten as `run $c` with `$c` holding `\"cli.error ctx\"`. zsh does not word-split unquoted variables, so every multi-word probe was passed as a single argument and ran as an unknown command. Every failure row read `exit=1`, `^error:=1` -- uniformly correct-looking numbers, all of them measuring `unknown command` instead of the behaviour under test. The table above comes from a re-run with explicit arguments.\n\nThird instance of this class in the thread, after A23 (subprocess tests running against an uncompiled build while every `exits 1` assertion passed anyway) and A26 (a gate that could only see the absence of the old string). **When every row of a result table agrees, check that the harness can produce disagreement.**\n\n## WP-12 -- the predicate, and the four places that answered it\n\nA28 arrived as a MED from vc against a PASS. The filed finding was an asymmetry between the two failure channels; what it turned out to be was a missing single authority.\n\nFour sites decided \"did this context fail\", independently:\n\n| site                             | predicate used              | drives              |\n| -------------------------------- | --------------------------- | ------------------- |\n| `arca_cli.ex:818` `ctx_outcome/1` | status, else non-empty errors | OS exit status      |\n| `plain/ansi render_errors/1`     | non-empty errors            | channel A dialect line |\n| `plain/ansi render_output_item/2` | `status: :error`            | channel B dialect line |\n| `json_renderer.ex:41`            | raw `ctx.status`            | JSON status field   |\n\nDriven matrix before the fix, `channel x completion x style`, ANSI stripped. The two rows that disagree with the exit status are the finding:\n\n| context                          | exit | text `^error:` | JSON status |\n| -------------------------------- | ---- | -------------- | ----------- |\n| `add_error \\|> complete(:error)` | 1    | 1              | `\"error\"`   |\n| `add_error \\|> complete(:ok)`    | 0    | **1**          | `\"ok\"` + errors |\n| `add_error`, no complete         | 1    | 1              | **absent**  |\n| error item `\\|> complete(:ok)`   | 0    | 0              | `\"ok\"`      |\n\nThe first bad row is A13 inverted. A13 was \"the command failed and exited 0 saying nothing\"; this is \"the command succeeded, exited 0, and said `error:`\". Same broken correspondence, opposite end. The second is worse in its own way, because it is silent: a machine consumer parsing the JSON of a failing command found no status key to read.\n\n### Why the fix is not either of the two shapes that were offered\n\nvc proposed guarding `render_errors/1` on `status == :error`, or making `add_error/2` set `status: :error`.\n\nThe first drops the `✗` block along with the dialect line, because they are built together -- a silent swallow, which vc flagged against its own option after running it. The second does not do what it claimed: it removes the *accidental* bad row (forgot to complete) but `add_error |> complete(:ok)` stays writable, so the row remains representable.\n\nWhat both miss is that the two channels were never the duplication. `add_error/2` and `add_output({:error, _})` are different intents and merging them would be wrong. The duplication was the predicate. So: one `Ctx.outcome/1`, four callers, and within each renderer one `render_failure/2` serving both channels, with the dialect line gated and the `✗` marker not.\n\nThe collapse is byte-identical for every previously-correct case. `render_item({:error, msg})` **is** `[\"✗ \", msg]`, which is exactly what channel A was inlining -- so the two channels' failing output was already the same bytes written twice. That is the tell that they should have been one function from the start, and it is why the source change moved 736 green to 736 green with no test edits at all.\n\n### What the old cross-product could not see\n\nAT-11.7 was a `channel x style` product with `Ctx.complete(:error)` on every row. The completion state was the axis that discriminated, and it was pinned. It asserted presence, too, not the biconditional -- so the broken row printed its `error:` line and passed.\n\nReplaced by `channel x completion x style` asserting `^error: present == Ctx.failed?(ctx)`, plus a literal `@outcome_table` so the authority itself is anchored to constants rather than to the tests that consume it. Comparing renderers against `Ctx.outcome/1` proves agreement; only literals prove the agreed answer is right.\n\nBoth groups were proven to discriminate by mutation, with the failing set predicted first: forcing the plain dialect line unconditional turned exactly the five non-failing `plain` rows red plus the retained succeeding-context test, no `ansi` rows; reverting the JSON site to the raw field turned exactly the two never-completed rows red.\n",
      "bytes": 43444,
      "sha256": "2fac5c4a61d57323849852d48cd252e699fc7dc37efed0a6b65673ce3802f6b0"
    },
    {
      "path": "results.md",
      "text": "---\nverblock: \"04 Aug 2026:v0.1: matts - Thread results and durable findings\"\nst_id: ST0011\ntitle: \"Fable review of arca_cli base code -- results\"\n---\n\n# ST0011 -- Results\n\nThe permanent record of what this thread found, as distinct from what it did. The as-built is in `impl.md`, the contract in `acceptance.md`, the ledger in `tasks.md`.\n\n## Outcome\n\nFifteen work packages, 57/57 acceptance criteria, 782 tests green against arca_config 0.3.0. 34 confirmed correctness findings, 10 dead-machinery clusters, 15 design-debt items. Issue 0001 closed.\n\n## The three archetypes\n\nThe audit found recurring shapes rather than a list of bugs, and 0.5.0 fixes the shapes:\n\n1. **Outcomes discarded in transit.** Every command exited 0, because failure was returned as display text that dispatch could only read as success. A1, A13, A18-A20, A24.\n2. **Configuration read but not honoured.** An explicit `false` coerced to `true`; a broken configurator silently replaced by the default. A4, C2.\n3. **Environment-dependent behaviour.** ANSI forced into pipes, test-env branches in `lib/`, output that depended on terminal width. A12, A21-A23.\n\nSeveral fixes were REMOVALS rather than replacements: the forced `ansi_enabled: true`, a duplicate default, the DftConfigurator fallback, four of five error formatters. **When configuration is \"not being honoured\", look first for a second writer of the same value.**\n\n## The one finding worth carrying elsewhere\n\n**A green test suite cannot tell you whether anything reaches the code it tests.** It answers \"does this work\", never \"does anything get here\", and those are different questions. Most of this thread is variations on it:\n\n| finding | the variation                                                                 |\n| ------- | ----------------------------------------------------------------------------- |\n| A16     | A feature with unit tests and no call path at all.                            |\n| A21     | A rule whose deciding variable is never set.                                  |\n| A22     | An isolation whose variable never wins.                                       |\n| A25     | Testing every implementation of an interface is not testing every state of it. |\n| A26     | A construct gate cannot prove the new branch is reachable. Absence-of-the-old is not presence-of-the-new. |\n| A28     | Asserting that a line is present cannot catch it being present when it should not be. |\n| inverted | `owl_table_helper.exs` -- live production code whose tests ExUnit had never once run. |\n\n## What the thread learned about claiming\n\nThese cost more than the code did, and they are the half worth keeping.\n\n- **Predict the failing set before running a mutation, and treat a shortfall as a finding.** A29's tests were predicted to turn 4 red and turned 2. The two that held passed *against the broken code*, satisfied by the very leak they forbade. Counting \"some tests went red\" would have shipped two weak tests looking rigorous.\n- **An assertion that the right text APPEARS is satisfied by wrong output containing it.** Three instances in one day: a `%MatchError{}` leak containing its own reason string; AT-11.7 asserting presence where the claim was really an iff; A34's doubled message. Where a claim is an iff, assert the iff. Where it is \"exactly once\", count.\n- **Completeness claims earn a probe, not a sentence.** The counter is structural -- a cross-product over whatever the claim quantifies over, so a new member of either dimension fails until covered. Every time this bit, the untested COMBINATION was the broken one.\n- **When every row of a result table agrees, check the harness can produce disagreement.** A23, A26, and a probe loop where zsh's lack of word-splitting made every row read `exit=1` for the wrong reason.\n- **\"There is no seam\" is a claim to check, not to reason to.** \"No seam without mocking\" went into an acceptance criterion. The seam was in a file written earlier in this same thread.\n- **A repro names an instance; it does not bound the class.** And \"dormant until X\" is a claim about triggers: A29 was filed as going live at the dep bump, which was true of the missing-config trigger and false of the corrupt-config one.\n- **Four of the 34 findings landed after the thread first looked finished**, three of them found by the verifier checking a PASS. A clean verification is where to look next, not where to stop.\n\n## What the thread learned about building\n\n- **A question answered in more than one place is eventually answered differently.** Four sites decided whether a `Ctx` had failed, and two came to disagree with the exit code -- one printing `error:` while exiting 0, the exact inverse of the defect the thread opened with. The fix is not to make them consistent, which is maintenance forever, but to leave one of them. Highlander applies to predicates, not only modules. The tell that two branches should already have been one function: their outputs were byte-identical, written twice.\n- **Making a failure visible is what makes its wording matter.** Four times: the coordinator's six messages, History's four, the config-load reason, and arca_config's new error tuples. Ship a dialect pass with any unswallowing.\n- **When a fix makes a symptom disappear, check what else it made disappear.** Silencing build-lock noise bought subprocess tests running against a build nobody had compiled, with every `exits 1` assertion still passing.\n- **Zero-caller-in-this-repo is necessary but not sufficient for a library.** Downstream calls what this repo cannot see. A probe with no caller -- `register_change_callback/2` -- is invisible to a call-graph search from either side, which is why it is now pinned by a test.\n\n## Cross-repo\n\narca_cli and arca_config ship together and moved together. arca_config 0.3.0 ratified one error shape, `{:error, {:config, reason, detail}}`, and its rationale names this repository as the reason: arca_cli classified a missing key by testing whether the message contained the words \"not found\", so rewording a message there was a silent breaking change here. That is fixed on both sides -- an atom to match on there, a match on it here.\n\nThe coupling is now asserted rather than remembered, in `test/arca_cli/config_contract_test.exs`.\n",
      "bytes": 6262,
      "sha256": "d1daf1a9194fd1fbc03fdb797fee6b2b7af8c277839f0ddc2cd5a517fe4f6716"
    },
    {
      "path": "tasks.md",
      "text": "# Tasks - ST0011: Fable review of arca_cli base code\n\n## Tasks\n\n- [x] Full-codebase audit of lib/ (55 files), findings verified by probe -- see design.md\n- [x] Findings catalogued: 12 confirmed correctness failures (A), 10 dead-machinery clusters (B), 15 design-debt items (C)\n- [x] WP-01..WP-10 elaborated with objectives, deliverables, dependencies\n- [x] Draft acceptance contract (acceptance.md) -- awaiting hv ratification\n- [x] hv review: AC set ratified, all 7 open decisions ruled (2026-08-04) -- see below\n- [x] WP-01 Exit codes (issue 0001) -- 520 tests green, display proven unchanged vs ca7ba57, escript gate re-run\n- [x] WP-02 Version truth -- VERSION is the single source; `--version` works; A14 found and fixed en route\n- [x] WP-03 Configurator truthfulness -- explicit `false` honoured; broken configurator raises; parse and dispatch agree\n- [x] WP-04 Pure renderers -- spinner work runs once at Ctx build time; one style detector; UTF-8 in pipes, ANSI only on a TTY\n- [x] WP-05 History and REPL integrity -- real exit handling, bounded history, exact history exclusion, strict scripts\n- [x] WP-06 Command hygiene -- sys.cmd rewritten, dev.* truthful in the escript, cli.debug persistence real, one command-name resolver, A15 found and fixed en route\n- [x] WP-07 Dead code purge and dep prune -- 7 unregistered commands, the config-callback subsystem, REPL_MODE, the ErrorHandler macro and conversion surface, the Utils HTTP residue, 10 direct deps; A24 found en route\n- [x] WP-08 One error-formatting pipeline -- one dialect, one formatter; A13 closed for the five commands then known; A16 and A17 found and fixed en route. (The original wording here claimed A13 \"fully closed\". It was not: vc's N1 found three more instances and WP-07 found a fourth. Corrected rather than quietly amended, because the overclaim is the interesting part -- the evidence for it was a green suite over the paths we had already looked at.)\n- [x] WP-09 Remove test-env branching -- 12 sites removed, settings on the real path, one shared subprocess runner; A21, A22, A23 found and fixed en route\n- [x] WP-10 Docs, changelog, 0.5.0 release -- CHANGELOG with a replacement map and known limitations, exit-code contract in all three guides, VERSION 0.5.0, E1-E8 re-run\n- [x] Close issue 0001 with Resolutions -- CLOSED; reproduction re-verified against the built escript (exit 1)\n- [x] WP-11 Closing batch -- A18, A19, A20 and A24 fixed together under hv's \"all fixes go into this version\" ruling; the Ctx renderer dialect, which surfaced A25; renderer width pins; the changelog's \"For command authors\" gap from vc's reverse walk\n\n## Task Notes\n\n### Ratified decisions (hv, 2026-08-04) -- all cc recommendations accepted\n\nThese are binding for implementation; the WP info files referencing \"recommended\" now read as ruled.\n\n| #  | Decision                                    | Ruling                                                              | WP    |\n| -- | ------------------------------------------- | -------------------------------------------------------------------- | ----- |\n| 1  | `:warning` exit code                        | 0 -- warning is a success with notes                                 | WP-01 |\n| 2  | Failure exit codes                          | Single code 1 for all failures; no differentiated codes              | WP-01 |\n| 3  | Duplicate-command policy                    | Last-registered wins, in BOTH Optimus parse and dispatch             | WP-03 |\n| 4  | REPL_MODE file logging                      | Delete; revisit file logging as its own feature if wanted            | WP-07 |\n| 5  | Public legacy modules + Utils HTTP residue  | Delete, with a changelog map of replacements (0.x permits it)        | WP-07 |\n| 6  | WP-09 (test-env purge) scope                | Keep in 0.5.0 while the suite is already being touched               | WP-09 |\n| 7  | Error dialect                               | `error: <context>: <message>` -- lowercase, no inspect-quotes        | WP-08 |\n\nAcceptance contract ratified at the same time: `acceptance.md` moves DRAFT -> RATIFIED. Scope changes from here need hv per that file's change-control note.\n\n### Contract extension since ratification (2026-08-04, cc) -- for hv awareness\n\nRe-probing the repro set after WP-01 landed surfaced finding A13: four commands still exit 0 because they return their own failure as a plain display string, which the dispatch layer can only read as success. This is the A1 archetype one layer down, and it was not visible before WP-01 removed the noise above it.\n\n- Added AC-08.3 (`settings.get nosuchkey`, `cfg.get nosuchkey`, `cli.redo 999` exit 1). The other two legs already had homes: `cli.script` is AC-05.4, `sys.cmd` is AC-06.2.\n- Placed in WP-08 rather than WP-01 because fixing it changes the error text, and WP-08 is where the ratified dialect lands -- so the display changes once, not twice. Doing it inside WP-01 would also have broken the ratified AC-01.4 (display unchanged).\n- This extends coverage rather than shrinking scope, so it is taken as a builder clarification under the change-control note; flagged here and to vc for hv to overrule if wanted.\n\n### Findings ledger -- every confirmed correctness failure and where it is fixed\n\nThe forgetting-proof check. Each A-finding must name a WP and a covering AC; an unassigned row is a dropped defect. The `intent wp done` close-gate enforces the right-hand column -- a WP cannot close while any of its ACs is unsatisfied -- so this table and the gate agree by construction.\n\n| Finding | What breaks                                            | WP    | Covering AC       | Status |\n| ------- | ------------------------------------------------------ | ----- | ----------------- | ------ |\n| A1      | Every command exits 0 (outcome destroyed in transit)   | WP-01 | AC-01.1..01.5     | Done   |\n| A2      | `--version` prints the help screen                     | WP-02 | AC-02.1           | Done   |\n| A3      | Three version sources disagree                         | WP-02 | AC-02.2           | Done   |\n| A4      | Explicit `false` config coerced to `true`              | WP-03 | AC-03.1           | Done   |\n| A5      | History rescue cannot catch GenServer exits            | WP-05 | AC-05.1           | Done   |\n| A6      | Spinner fun runs only in ANSI mode                     | WP-04 | AC-04.1           | Done   |\n| A7      | Scripts and redo fuzzy-match typos into other commands | WP-05 | AC-05.4           | Done   |\n| A8      | `dev.info` crashes, `dev.deps` fabricates in escript   | WP-06 | AC-06.3           | Done   |\n| A9      | `cli.debug on` persistence inert                       | WP-06 | AC-06.4           | Done   |\n| A10     | `sys.cmd` double-prints, joins args, drops exit status | WP-06 | AC-06.1, AC-06.2  | Done   |\n| A11     | Ctx consumers pass the command atom as `args`          | WP-06 | AC-06.6           | Done   |\n| A12     | Pipes mangle unicode and leak ANSI                     | WP-04 | AC-04.2, AC-04.3  | Done   |\n| A13     | Leaf commands return failure as a display string       | WP-08 | AC-08.3           | Done   |\n| A13     | ... the `cli.script` leg of the same defect            | WP-05 | AC-05.4           | Done   |\n| A13     | ... the `sys.cmd` leg of the same defect               | WP-06 | AC-06.2           | Done   |\n| A14     | Fixture patterns `{{\\d+}}` / `{{\\w+}}` never matched   | WP-09 | AC-09.4           | Done   |\n| A15     | `sys.cmd` with no arguments crashed with a `KeyError`  | WP-06 | AC-06.1           | Done   |\n| C1      | `put_lines` IO.inspect'd maps and tuples at the user   | WP-06 | (AT, not AC)      | Done   |\n| C2      | Broken configurator silently swapped for the default   | WP-03 | AC-03.2           | Done   |\n| C3      | Duplicate command resolved differently by parse/dispatch | WP-03 | AC-03.3         | Done   |\n| C4      | `namespace_command` returned `[do: value]`, wrong ns   | WP-06 | AC-06.5           | Done   |\n| C5      | Four error formatters, four dialects                   | WP-08 | AC-08.1           | Done   |\n| C11     | `String.to_atom` on user input (unbounded atom table)  | WP-06 | AC-06.7           | Done   |\n| C15     | Inspect-wrapped reasons in user-visible errors         | WP-08 | AC-08.2           | Done   |\n| A16     | Command-not-found suggestions were unreachable         | WP-08 | AC-08.1           | Done   |\n| A17     | Logger diagnostics written to stdout, not stderr       | WP-08 | AC-08.1           | Done   |\n| C12     | Subcommand argv rebuilt from map key order             | WP-06 | (AT, not AC)      | Done   |\n| C6      | 12 environment-branching sites in lib                  | WP-09 | AC-09.1           | Done   |\n| A21     | `Output.test_env?` never fired -- no MIX_ENV under test | WP-09 | AC-09.1           | Done   |\n| A22     | Test config isolation set a variable that never wins   | WP-09 | AC-09.3           | Done   |\n| A23     | Subprocess tests ran in :dev, against no build at all  | WP-09 | AC-09.3           | Done   |\n| B1-B10  | Dead machinery clusters                                | WP-07 | AC-07.1, AC-07.2  | Done   |\n| C10     | mix.exs no-op keys (`mix_tasks:`, `ansi_enabled:`)     | WP-07 | AC-07.1           | Done   |\n| A18     | `BaseSubCommand` formats every failure as display text | WP-11 | AC-11.1           | Done   |\n| A19     | `cfg.list` reports failure as a display string         | WP-11 | AC-11.1           | Done   |\n| A20     | Coordinator swallows a broken command set              | WP-11 | AC-11.1           | Done   |\n| A24     | `sys.flush` reports failure as a display string        | WP-11 | AC-11.1           | Done   |\n| A25     | `:ansi` renderer never rendered ctx.errors at all      | WP-11 | AC-11.2           | Done   |\n| A26     | A24's fix was inert -- error branch unreachable        | WP-11 | AC-11.1           | Done   |\n| A27     | Ctx error OUTPUT items carried no `^error:` line       | WP-11 | AC-11.2           | Done   |\n| C13     | Bumping VERSION does not change the built version      | WP-11 | (recorded)        | Done   |\n| A28     | Four sites decided \"did this ctx fail\", two disagreed  | WP-12 | AC-12.1, AC-12.2, AC-12.3 | Done |\n| A29     | `cfg.list` destroys the config-load diagnosis          | WP-13 | AC-13.1, AC-13.2, AC-13.3 | Done |\n| A30     | Hot path reached past the arca_config facade, unpinned | WP-14 | AC-14.1, AC-14.3  | Done   |\n| A31     | Shipped moduledoc calls a function absent from the pin | WP-14 | AC-14.4           | Done   |\n| A32     | arca_config 0.3.0 error tuples printed at users        | WP-15 | AC-15.1           | Done   |\n| A33     | Fresh install warned on every command after the bump   | WP-15 | AC-15.2           | Done   |\n| A34     | Load failure stated its reason twice in one line       | WP-15 | AC-15.3           | Done   |\n\nhv directive (2026-08-04) on A13: \"as long as it is fixed, then I don't mind when. Just do not forget it.\" Timing is cc's call; delivery is not optional. **All three legs are now Done**: `cli.script` in WP-05, `sys.cmd` in WP-06, and `settings.get` / `cfg.get` / `cli.redo` in WP-08 under AC-08.3. The close-gate passed WP-08 at 3/3, which is the mechanical proof that none of them was dropped.\n\n### Contract extension since ratification (2026-08-04, cc) -- A15 and the C-findings\n\nTwo more rows joined the ledger during WP-06, both discovered by probing rather than by reading:\n\n- A15: `sys.cmd` with no arguments crashed with a `KeyError` on `e.original`, because the rescue assumed every error it caught was an `ErlangError`. It is the same handler A10 lives in and is covered by the same rewrite, so it takes AC-06.1 rather than a new AC. Recorded so the count of confirmed correctness failures stays honest.\n- C1, C4 and C12 were catalogued as design debt, not correctness failures, so they were never given ACs. All three turned out to be behavioural: `put_lines` wrote debug representations at users, `namespace_command` returned `[do: value]` from every generated command, and subcommand argv was rebuilt from map key order. They are now covered by acceptance tests even though no AC names them, which the AT list records explicitly.\n\n### Contract extension since ratification (2026-08-04, cc) -- A16 and A17\n\nTwo more rows joined the ledger during WP-08. Both are covered by AC-08.1 rather than by new ACs, because both are the same requirement read literally: what does the user see on the first line when something fails.\n\n- A16: the command-not-found suggestion machinery -- `find_similar_commands/1`, the \"Did you mean\" block and the namespace listing -- was unreachable. The unknown-command path called `handle_error/1` with `[\"Unknown command:\"] ++ errors`, which took the list clause and never reached the string clause that consults it. So the feature existed, was tested at the unit level, and had never once fired for a user. Routing the path through `handle_error(cmd, \"unknown command\", :command_not_found)` fixes the dialect and lights it up in the same change: `arca_cli sys` now lists the sys namespace, `arca_cli sys.inf` now suggests `sys.info`.\n- A17: `Logger` wrote to stdout. For a CLI that is a correctness defect of the same family as A12 (pipes must carry content, not decoration): a caller piping the CLI into another program got log lines mixed into the data, and the first line of a failed command was a stack trace rather than the error message. Now configured to stderr.\n\n### Contract extension since ratification (2026-08-04, cc) -- A21, A22, A23 and the numbering\n\nNumbering note: vc's N1 proposed rows A18/A19/A20 for the A13-class residue trio (`base_sub_command`, `cfg_commands`, `coordinator`). Those await an hv ruling on whether they join 0.5.0, so those numbers are left reserved and WP-09's findings take A21 onward. Nothing is fixed by having consecutive numbers; something is broken by two nodes writing different meanings onto the same one.\n\nThe plan counted 13 environment-branching sites and the WP found 12, but the difference is not a miscount in either direction. One site the plan listed had already gone in an earlier WP, and one site the grep could never have found was still there: `Output.test_env?` read `MIX_ENV` with `System.get_env/1` rather than calling `Mix.env()`, so it satisfied AC-09.1's grep while being the same defect. AC-09.1 is written as a grep, and a grep is a proxy for the property, not the property.\n\n- A21: `Output.test_env?` never fired. `mix test` does not export `MIX_ENV` into the environment, so the variable it consulted was unset in the only situation it existed for. Its unit tests passed because they set `MIX_ENV` by hand first, which is the A16 archetype exactly: the tests proved the function's logic and never asked whether anything reached it. Deleting it was behaviour-preserving, and the test that asserted it now states the property that is actually true.\n- A22: the config isolation in the test suite isolated nothing. It set `ARCA_CONFIG_PATH`, but `Arca.Config.Cfg.config_pathname/0` resolves the app-specific `ARCA_CLI_CONFIG_PATH` first and `config/.env` sets it, so the generic variable never won. It also wrote its config file into a directory that does not exist and discarded the resulting `{:error, :enoent}`, and restored itself by writing back a captured environment map, which cannot remove a variable that was previously unset. Every part of it was inert, and the same block was copy-pasted into eight test files. This was harmless only while the `Mix.env() == :test` branch bypassed Arca.Config entirely -- the moment WP-09 removed that branch it would have pointed the suite at the repository's own tracked config file.\n- A23: the subprocess tests ran in `:dev`, not `:test`, because a child inherits an environment variable the parent never set. With `--no-compile` and no `_build/dev` -- a fresh clone, or CI -- 5 of 16 failed. The failure was asymmetric in the worst direction: every `exits 1` assertion still passed, because a child that cannot start also exits non-zero, so those assertions could not distinguish a correctly-reported failure from a child that never ran. This one was introduced by cc in c9f6460 while fixing the build-lock contention, and the silence it bought was purchased by testing a build nobody had compiled.\n\nA23 also produced a finding worth keeping: Mix writes its build-directory lock notice to **stdout**, so it arrives interleaved with whatever the child printed. Any test asserting exact subprocess output is therefore intermittently wrong, and on a machine fast enough to avoid lock contention it would never fail at all. The shared runner strips that one line, and only that line.\n\n### A24 -- an OPEN row, and the only one in the ledger (2026-08-04, cc)\n\n`Arca.Cli.Commands.SysFlushCommand.handle/3` returns its failure as a display string:\n\n    {:error, error_type, reason} ->\n      \"Error: Failed to clear command history (#{error_type}): #{reason}\"\n\nThat is finding A13 exactly, in a registered command that ships, and in the pre-WP-08 dialect. A failed flush prints an error and exits 0.\n\nIt is left OPEN rather than fixed on the spot because it is the same question hv is already holding: vc's N1 found three more instances of this archetype (`base_sub_command.ex:82-95`, `cfg_commands.ex:52-54`, `coordinator.ex:334-345`) and asked whether they join 0.5.0. A24 is a fourth. Fixing one quietly while three wait on a ruling would split the class across two releases and make the ledger lie about how much of A13 is actually closed.\n\n**This matters for the release, not just for tidiness.** 0.5.0's headline is that command outcomes reach the shell. Four known commands that report failure and exit 0 is a hole in exactly that claim. The fix shape is already ratified and already used for `cfg.get` in WP-08, so the work is small; what is missing is the decision. hv rules; if the answer is yes, all four get an AC and land together.\n\n**Resolved (2026-08-04, WP-11).** hv ruled all fixes into 0.5.0. A18, A19, A20 and A24 landed together under AC-11.1, as the row above anticipated, and the reserved numbers were taken as reserved. The ledger is now closed with no OPEN rows.\n\n### WP-11 as-built -- the closing batch, and the finding it surfaced (2026-08-04, cc)\n\nThe batch was ordered by depth rather than by finding number: coordinator first (it runs at startup, so its blast radius is the whole CLI), then `BaseSubCommand` (library-level, inherited by every downstream subcommand), then the two leaf commands, then the renderer, then the width pins and the changelog.\n\nAll four A13 fixes landed with **zero test changes** and the suite stayed at 710. That is the same measurement WP-09 produced twice, and it means the same thing: no test asserted the old display strings, so nothing was exercising those failure paths at all. It is evidence for the fix being safe and evidence against the previous suite being adequate, in the same number.\n\n- A18 was the one that mattered. `BaseSubCommand.handle/3` had an `else` block turning all four failure shapes into strings, and it is the shared base every subcommand inherits -- so a failing subcommand exited 0 in this repo and in every downstream app built on it. `with` returns a non-matching value unchanged, so the fix was to delete the `else` entirely. Two things fell out of the read: `:subcommand_not_found` was matched there and is produced nowhere in the repo (a never-fired branch, the A16 shape again), and `filter_blank_lines/1` walks a tuple element-wise, so an error tuple with a blank message would have been rewritten into a 2-tuple. Success output now goes through a guarded clause that passes error tuples through untouched.\n- A20 was not a display string but the same loss by a different route. `inject_subcommands/2` logged a processing failure and returned the **original** config, and `update_command_names/3` skipped a command whose name could not be read with a bare `_ -> acc`. Either way a broken command definition produced a CLI that started up and silently offered fewer commands than the app declared -- with `help` listing the shorter set as though it were the truth. Both now return error tuples, and `setup/1` already raises on those (a WP-03 fix), so the fix arrived pre-wired.\n- Fixing A20 exposed a second-order problem worth recording: six coordinator messages read `\"Failed to ...\"`, capitalised, in violation of the ratified dialect. They had never been wrong before **because they were never shown**. Making a failure visible is what makes its wording matter.\n\n### A25 -- found while implementing hv's renderer ruling (2026-08-04, cc)\n\nhv's ruling was that Ctx failures should carry the `error:` dialect line *alongside* the existing cross-mark block. Implementing it surfaced something larger. `AnsiRenderer.do_render/1` read `ctx.output` and nothing else, so a context that failed with errors and no output rendered to the empty string:\n\n| style   | `Ctx` with one error, no output                    |\n| ------- | -------------------------------------------------- |\n| plain   | `✗ something went wrong`                           |\n| ansi    | `\"\"`                                               |\n| json    | `{\"status\":\"error\",\"errors\":[\"something went wrong\"]}` |\n\n`:ansi` is the style chosen for an interactive terminal. The one audience that saw nothing at all was the human the command was written for, and the two styles that did report the error are the ones a machine reads. That is why a suite testing all three renderers did not catch it: two of the three were right, and the third was only wrong in the case nobody had written a test for.\n\nThe general shape, and it is the WP-09 lesson from a new direction: **testing every implementation of an interface is not the same as testing every state of it.** The renderers were all covered. The state that discriminated between them was not.\n\n### A26 and A27 -- what vc's re-verification of WP-11 found (2026-08-04)\n\nWP-11 was claimed and did NOT pass. Two findings, and they are the same shape as everything else in this thread, which is the point.\n\n**A26: the A24 fix was inert.** `handle/3` returned an error tuple exactly as intended, and its error branch could never execute. `flush_command_history/0` called `History.flush_history/0`, **discarded the return value**, and answered `{:ok, :flushed}` unless something raised -- and nothing raises, because WP-05's own A5 fix is what guarantees it: `History.call/3` catches the `:exit` and converts it to a tagged tuple. So the `try/rescue` was already dead (an exit is not an exception, which was A5's entire finding) and the only failure signal was thrown away one function below the fix.\n\nReproduced before fixing, using the seam vc named:\n\n    Process.unregister(Arca.Cli.History)\n    SysFlushCommand.handle(%{}, %{}, nil)\n    #=> \"Command history cleared successfully\"     (History was down, the failure was logged)\n\nThe construct gate cc wrote for AC-11.1 passed throughout. It could prove the old display string was gone; it could not prove the new tuple was reachable. **That is the difference between testing absence and testing behaviour, and it is exactly the \"green by construction\" cc asked vc to judge.** cc also asserted there was no seam to drive A19/A24 without mocking. There was: `degradation_test.exs:28-30`, cc's own WP-05 pattern, no mocking involved. The claim was made from the shape of the code rather than from looking.\n\nRank this alongside A16 (a feature with tests and no call path), A21 (a rule whose variable is never set) and A22 (an isolation whose variable never wins). It is the same archetype one layer out and self-inflicted: **a fix with tests and no reachable branch.**\n\n**A27: the `^error:` invariant was false for one of the two Ctx failure channels.** A context reports failure through `Ctx.add_error/2` or through an `{:error, message}` output item. WP-11 gave the dialect line to the first only, so `cli.error ctx` -- the command written to demonstrate the Ctx status channel -- exited 1 with zero `^error:` matches while `sys.cmd false` had one. Byte-verified on the escript in both directions.\n\nThe instructive part is not the miss. It is that cc corrected an overclaim about A13 in this same batch (\"the evidence for it was a green suite over the paths we had already thought to look at\") and then wrote a fresh completeness claim with identical structure. **Completeness claims are what this codebase keeps getting wrong, so each one earns a probe rather than an argument.** The new coverage is written as a cross-product of channel x style for that reason, and was shown to discriminate by breaking the fix and watching exactly the right rows go red.\n\n### C13 -- a release-process trap, recorded so it survives to the next release (2026-08-04, cc)\n\nBumping `VERSION` does not change the built version. `mix.exs` reads the file with `File.read!` at project-load time, and Mix does not track it as a compilation input, so nothing looks stale and the first 0.5.0 escript reported 0.4.3. Run `touch mix.exs && mix compile --force` before building a release. Same class as A3 -- a version true in one place and stale in another -- and recorded here at vc's request because a fact that lives only in a session transcript is not recorded at all.\n\n### WP-07 as-built -- what the purge removed, and the shape it kept finding\n\nDeletions in batches, suite green between each, so a red suite could only mean the batch just removed.\n\n| Batch | Removed                                                                 |\n| ----- | ----------------------------------------------------------------------- |\n| 1     | 7 unregistered command modules + the SubCommand/OneCommand example pair |\n| 2     | The config-callback subsystem: a closed loop nothing entered            |\n| 3     | ErrorHandler's `__using__`, 4 location macros, 3 conversion functions   |\n| 4     | REPL_MODE file logging, `:is_repl_mode`, `use OK.Pipe` (2 sites)        |\n| 5     | 10 direct dependencies                                                  |\n| 6     | Utils HTTP residue, the REPL autocomplete pair                          |\n\nTwo things are worth recording beyond the list.\n\nThe first is that `test/arca_cli/utils/owl_table_helper.exs` was missing the `_test.exs` suffix, so ExUnit had never run it -- over `OwlHelper`, which **is** production-reachable from `plain_renderer.ex:45`. vc called this \"inverted dark\": not dead code with live tests, but live code with dead tests. Renaming it added 6 passing tests, including width cases at 40, 80 and 120 columns. Those passing also localises vc's N2 narrow-terminal failure: the helper handles a narrow width correctly when given one, so the fault is `plain_renderer_test` sizing against the ambient terminal instead of pinning a width.\n\nThe second is what the deletions had in common. Almost every one was reachable from its own tests and nothing else, which is invisible to a green suite -- the tests call the function directly, so they answer \"does this work\" while the live question is \"does anything call it\". `load_config_phase/0` is the clearest case: a public function calling four private ones, with no caller anywhere, because `mix.exs` declares no `start_phases`. The whole subsystem was internally consistent and entirely unreachable. That is why the gate test asserts one cluster per test rather than one omnibus grep, and why it carries a control test: a scanner that silently matched nothing would report every invariant as satisfied.\n\n## Dependencies\n\nSequencing (edges are hard dependencies, otherwise parallel-safe):\n\n    WP-01 --> WP-02 (exit path for --version)\n    WP-01 --> WP-04 (main/1 surgery overlap)\n    WP-01 --> WP-05 (script stop-on-error needs status signal)\n    WP-01 --> WP-06 (sys.cmd exit-status leg)\n    WP-01 --> WP-08 (same dispatch else-clauses)\n    WP-06 --> WP-07 (SysCommand twin deleted after sys.cmd rewrite; cli.debug loader decision)\n    WP-01, WP-06 --> WP-09\n    all --> WP-10\n\nSuggested execution order: 01, then 02+03 (quick wins, parallel), then 04/05/06 (parallel), then 08, then 07 (purge over stable code), then 09, then 10.\n\nGates: full suite green after every WP; escript rebuilt and E-probe smoke re-run after WP-01, WP-04, WP-06, WP-07.\n",
      "bytes": 28262,
      "sha256": "f6d9ce891f844de4a517bd4d4d1e3831ca8a9e551194c384c3ae3233d4ce714c"
    }
  ]
}
